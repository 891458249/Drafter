# Drafter 版本更新记录

> 规则:所有代码改动与更新必须写入本文件(全局规则,见 ~/.claude/CLAUDE.md)。
> 按版本倒序,最新在最上;未发版标注「(未发布)」。

## v0.15.19(2026-09-23,未发布)
- 修复子 Agent 的创建、续聊、恢复和状态显示。
- 修复结构化 agentId 与实际模型登记，支持通过编号或名称继续发送消息。
- 修复子 Agent 日志路径及项目目录迁移，重启后可恢复模型校验。
- 统一默认模型、自定义固定模型与白名单规则；固定模型只在同 Key 会话生效，无需重复勾选。
- 预置 Agent 仅作为模板，不再默认全局执行；需要在扩展中复制并启用。
- 修复项目作用域、启用数量、配置热更新和待生效提示。
- 修复新版 Agent 与后台任务状态、再次运行状态和实际模型名称显示。
- 保留工具层与网络层的模型越权拦截。
- 验证：453 项完整回归测试通过，包含真实 SDK 配合本地模拟网关的创建、续聊、实际日志恢复验证。
- 安装后重启 Drafter 生效。

## v0.15.18(2026-09-23,已发布)
- 发布:GitHub Release [v0.15.18](https://github.com/891458249/Drafter/releases/tag/v0.15.18)(Release ID 394372298)已发布并设为 Latest;`Drafter-Setup-0.15.18.exe`(177,359,427 字节,比 v0.15.16 小 5.5MB)/blockmap(182,492 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.16 → latest 0.15.18, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.18.js` 已入库,本次上传一次通过(无 ECONNRESET)。产物 `dist/release-0.15.18/`。commit `9b09c1e2`(三会话改动合并 + 版本 bump),tag `v0.15.18`。
- 本次为**三个并行会话未发布改动的统一发版**:本体(=本小节 v0.15.18 拆分子任务判断)+ 下面两小节 v0.15.17(上下文压缩阈值/占用圈配色、打包面收敛/perMachine/签名预留)。v0.15.17 的两小节未单独出包,内容全部包含在本版中。
- 补记:package-lock.json 的 version 字段此前停留在 0.15.7(与实际版本脱节),本次一并对齐到 0.15.18。
- 需求(用户提出):**拆分子任务不能「无脑拆」**——拆分前先做一轮判断,把**能与其他会话并行**的任务开成子任务(新建会话),**需要等待**的不新建会话,保留在其需要等待的那个会话任务下。澄清后明确:①判断依据除子任务间依赖外,**还要带上项目组内其他并行会话的现状**(标题/是否进行中/最近在做),避免撞车;②被判定为「等待」的子任务,落地方式为**排入目标会话的消息队列,自动执行**(不建会话)。
- 核心设计:把「等待」统一抽象成**「排入某个目标会话的消息队列」**(`current` 本会话 / `session:<sid>` 其他并行会话 / `subtask:<index>` 本次的某个兄弟子任务)。目标忙就借 SDK 的流式输入队列天然排队,目标闲就立即执行——不需要额外的调度器,也不需要为「等待」新建会话。
- 判断层 `src/main/split-subtasks.js`(纯函数,不依赖 electron/DOM,便于单测):
  - `pickOtherSessions({currentSid, projectId, cwd, all, busyOf, doingOf, limit=6})`:挑出**同项目组**的其他会话(`projectId` 相同;会话没有 `projectId` 时用 `cwd` 兜底匹配),排除自身、已归档、素材类(`MEDIA_KINDS`),按 `updatedAt` 倒序取前 6 个,编号 `S1…S6`。
  - `sessionLine(label, s)` → `S1. <标题> [空闲]` / `S1. <标题> [进行中 / 最近在做:<最后一条用户输入>]`,即「其他并行会话现状」注入提示词的形态。
  - `buildSplitPrompt(requirement, {current, sessions})`:两步式提示词——**先判断**「哪些能与其他会话并行、哪些必须等谁(等本会话 / 等某个兄弟子任务 / 等某个已有会话)」,**再输出**严格 JSON 数组 `[{title, detail, mode, waitFor}]`,`mode ∈ parallel|wait`,`waitFor ∈ current|S1…|#1…`。
  - 容错解析:`normMode`(缺省或认不出一律按 `parallel`,避免因模型措辞差异把可并行的任务错判成等待)、`normWaitFor`(接受 `current` 及变体、`S1`/`#2`/裸数字、`子任务2`、`s_xxx` 会话 id)、`parseSubtasks`(从模型输出里抠出 JSON,失败给可读错误)。
  - `resolveWaitTargets(tasks)`:把 `waitFor` 解析成落点 `via`;**自引用、越界、成环一律退化为 `{kind:'current'}` 并置 cycle 标记**——宁可落到本会话排队,也不把任务丢掉。
  - `summarizePlan(tasks)`:弹窗小结用(「N 项可并行,N 项需等待」)。
- 主进程 `main.js`:
  - 新 IPC **`sess:splitSubtasks`**(main.js:1039):取当前会话的 Key/模型(缺省回退活跃 Key 的首个对话模型)→ `llmtext.complete` 发起判断 → `parseSubtasks`;返回 `{ok, tasks, sessions:[{key,id,title,busy}]}`。`sessions` 是**判断时的快照**,渲染端据此把 `S1` 换成本地会话 id,编号不会随会话列表变化而漂移。
  - 新 IPC **`sess:spawnSubtasks`**(main.js:1067):`resolveWaitTargets` 出计划后分两轮——**第一轮先给所有并行项建齐会话**(等待项可能正指向其中某个),继承当前会话的 `projectId/cwd/model/keyId/permissionMode/effort/agentModels/gemId`,标题加 `⧉ ` 前缀,`parentId` 指向当前会话;**第二轮按数组顺序投递**:并行项直接 `send`;等待项 `ts.send(detail, undefined, {emitEcho:true})` 排入目标队列,并写一条 `ui_aux`(忙:「需等本会话当前回合完成,结束后自动接着执行。」/闲:「等待目标已空闲,立即开始执行。」);目标被删/归档/解析不出时**回退新建会话**(`fallback:true`)而不丢任务。上限 12 项。
- `src/main/sessions.js`:`send(content, echoContent, opts)` 新增 **`opts.emitEcho`**——主进程代用户投递消息时(等待项排入本会话队列),目标会话界面不会自己加气泡,故持久化回显后补发一条 `ui_user_input` 实时事件(sessions.js:947)。
- 渲染层 `src/renderer/split.js`:弹窗里逐条可改「并行 / 等待」与等待目标(下拉分两组 optgroup:`#n` 本次其他子任务 + `session:<id>` 其他并行会话);`collectTasks` 按 DOM 重算 `#n` 的映射(用户可增删改行);确认后调 spawn 并按返回渲染结果清单(`.split-result-line`);`maybeSplitOnSend` 挂在发送路径上;保留 MutationObserver 兜底 Esc/点蒙层关闭(v0.15.15「弹窗误关」教训)。`src/renderer/input.js:25` 发送时先走 `maybeSplitOnSend(text)`(仅在开关激活且无附件时接管)。`preload.js:147-148` 暴露 `sessSplitSubtasks`/`sessSpawnSubtasks`;`src/index.html:464` 起为弹窗 DOM;`src/styles.css` 加 `.split-*` 样式(19 处)。
- 验证:**`npm test` 436/436 通过**(v0.15.17 基线 422,本次净增 14 例;`test/split-subtasks.test.js` 22 例覆盖 normMode/normWaitFor/parseSubtasks/pickOtherSessions/sessionLine/buildSplitPrompt/resolveWaitTargets(含自引用、越界、成环退化)/summarizePlan)。
- **端到端探针 `.claude-ui/probe-split-judge.js`(新增)全部 PASS**。构造:本地假 OpenAI 网关 + 真 Key(`protocol:'openai'` → agent 流量经 oai-proxy 翻译)+ 真 `electron.exe` + CDP,独立 `DRAFTER_USERDATA`。**关键手法**:假网关把当前会话的第一个回合**扣住不放**(`longGate`,由探针显式放行),于是能确定性验证「排队而非插队」——不依赖任何 sleep 赌时间窗。证据链:
  - 判断提示词确实带上了同项目组其他会话现状:`S1. 冒烟-同项目组其他会话B [空闲]`;
  - 混合判断结果在卡片上正确呈现(1 并行 + 1 等兄弟子任务 + 1 等当前会话),等待目标下拉显隐正确(并行项隐藏),小结「1 项可并行,2 项需等待」;
  - 确认后**只为「并行」项新建 1 个会话**,两个等待项都没建会话,结果清单指向正确目标(「冒烟-主会话A」/「⧉ 搭建后端接口」);
  - 长回合未放行时:等待项的回显已在主会话历史(index 1)但**长回合与等待项都没有任何回复**(`replyLong=-1, replyQueued=-1`)——证明是排队、不是插队;并行项也没被误排进主会话;
  - **放行后**:长回合回复(index 3)先于排队项回复(index 5) → 确实「等本会话当前回合结束后自动执行」;
  - 新建会话内:兄弟子任务回复(index 3)先于「等它」的那条(index 5),先后关系保持。
- 探针踩坑(已修,值得记):①`claude.exe` 自己会发一条 `<session>…</session>` 的**流式「起标题」请求**,必须与真实回合区分,否则误吃闸门、误判次序;②SDK 会在我们的提示词**之后**再追加一条 user 消息(`Available agent types for the Agent tool: …`),所以「取最后一条 user 消息」会取错——必须**从后往前扫、取第一条带我们标记的消息**(`findTurnMarker`);③主会话跑过回合后标题会被自动标题写掉,而 `title.js` 的 `autoTitle` **只在标题为空时才写**(title.js:61),所以要在**第一次发送前**先 `sessRename` 占位,否则落地提示里的目标会话名无法辨识(假网关对非流式起标题请求只回 `OK`)。
- 调试偏差记录:受管入口仍是本项目既定坑(supervisor 先死 → Job 句柄关闭触发 `KILL_ON_JOB_CLOSE` 把长探针无异常硬杀),故按 v0.15.14 既定应对**隔离直跑 + finally 自清理**。跑完核对:CDP 端口 `9236` 无监听、无 `drafter-split-judge-*` 临时目录、无本探针 `electron` 进程(全机 `electron.exe` 计数 0);debug-runtime `status`/`cleanup`/`verify` 均 `{"ok":true,"count":0,"pending":[]}`。用户已安装的 Drafter 与其他会话进程未触碰。
- 文件:src/main/split-subtasks.js、main.js、src/main/sessions.js、src/renderer/split.js、src/renderer/input.js、src/index.html、src/styles.css、preload.js、test/split-subtasks.test.js、.claude-ui/probe-split-judge.js(新)。
- 注意:改动生效需**重启 Drafter 进程**。另:判断依据包含其他会话的实时现状,故同一条需求在不同时刻拆出的结果可能不同(这是需求要求的行为)。

## v0.15.17(2026-09-22,已发布—随 v0.15.18 一并发出)
- 需求(用户提出):①上下文压缩自动触发必须等到上下文到 **100%** 才触发;②上下文按钮旁的占用圈三档配色——**<30% 绿 / 30~80% 蓝 / >80% 红**;③**只要没到 100%,压缩只能由用户点「压缩」按钮手动触发,禁止自动触发**。
- 根因(实测):`src/main/context-compaction.js` 的 `compactionEnv` 从 v0.15.7 起把 `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` **上限钳到 80**(`current >= 1 && current < 80 ? current : 80`),即无论用户怎么配,最晚也会在 `window * 80%` 自动压缩,与「100% 才触发」直接冲突。
- 阈值语义(从随包 `claude.exe`(agent-sdk 0.3.220)提取的反编译 JS 确认,非猜测):`compactAt = min(floor(window * pct / 100), window - 13000)`,其中 `pct` 仅在 `(0, 100]` 内生效;`blockedAt = window - 3000`(超过即拒绝继续);`window - 20000` 起告警。故 `pct = 100` → `min(window, window - 13000) = window - 13000`,是 SDK 允许的**最晚**触发点,且与 SDK 原生默认值等价。
- 修复①③(`src/main/context-compaction.js`):`compactionEnv` 改为**恒定下发 `'100'`**,不再透传任何更低的覆盖值(用户环境里残留的低值同样被顶成 100——否则等于自动触发,与「禁止自动触发」矛盾)。`autoCompactEnabled: true` 保持不变:关掉它会让窗口写满后无法继续对话(那是 v0.15.7 修过的回归),现在 100% 才压缩已满足需求。手动压缩链路本来就只有一处(`#btn-compact` → `api.sessSend(sid, '/compact')`,仅在回合进行中禁用),无需改动。
- 修复②(占用圈配色):新增纯函数 `ctxRingColor(pct)` 于 `src/renderer/ctxwin.js`(沿用该文件既有的**双环境导出**模式:渲染端挂 `window.ctxwin`,主进程/单测走 `module.exports`),`src/renderer/app.js` 的 `updateUsageButton()` 改为调用它(替换原来 `>=90 红 / >=70 黄 / 其余 accent` 的三档)。边界归属:`30` 归蓝、`80` 归蓝、`81` 起红(用户未指定精确边界,已用注释与单测锁定);非数字/NaN 按 0 处理(绿)。
- 配色变量:`src/styles.css` 的 `:root` 新增 `--blue: #5b8def`(附注释说明用途),`src/renderer/themes.js` 为 light(`#2f6fd0`)/midnight/forest 三个主题补 `--blue`(dark 主题 `vars: {}` 走 `:root` 兜底,`ALL_VARS` 由主题键推导故能正确回退)。
- 验证:**全量 `npm test` 422/422 通过**(v0.15.16 基线 421,本次净增 1 例 = 新增的 `ctxRingColor` 测试)。单元测试 `test/context-compaction.test.js`(3/3,断言改为「环境其它变量原样保留 + `50`→`100` + `100`→`100`」)与 `test/ctxwin.test.js`(5/5,新增 `ctxRingColor:三档阈值` 9 条断言:0/29 绿、30/55/80 蓝、81/100 红、undefined/NaN 绿);**真实 SDK 端到端** `test/agent-route-live.test.js` 13/13 通过(本地假 Anthropic 网关 + 真 claude.exe),其中两条关键回归在 100% 阈值下仍通过:`✔ 真实运行时:code 自动压缩后保持会话继续对话 (5166ms)`、`✔ 真实运行时:chat 自动压缩后保持会话继续对话 (3649ms)`,以及 `提供方拒绝超长上下文后自动恢复`(code/chat)、`极速聊天手动压缩后可继续,失败不清空会话`,证明抬高阈值没有破坏「压缩后会话可继续」。
- 调试偏差记录:长时测试无法在受管入口(debug-runtime launch)下跑完——命中本项目既定坑「supervisor 先死 → Job 句柄关闭触发 `KILL_ON_JOB_CLOSE` 把目标进程树无异常硬杀,状态卡 `running`」。按既定应对处理:`node .claude-ui/reconcile-scope.js <scope>` 复核 PID 后改写 released → `cleanup` 与 `verify` 均返回 `{"ok":true,"count":2,"pending":[]}`(两次受管尝试的 2 条记录,当前 scope 下 count 0);测试改**隔离直跑**(`node --test ...`,不经受管 Job)。跑完核对:无 `D:\ClaudeUI\node_modules` 下的残留进程与端口占用(现存 `claude.exe` 全部属于用户已安装的 Drafter 与其他会话,未触碰)。新增包装脚本 `.claude-ui/run-tests-managed.js`(受管层不转发子进程 stdout,故由它自行 spawn 并落盘)。
- 文件:src/main/context-compaction.js、src/renderer/ctxwin.js、src/renderer/app.js、src/styles.css、src/renderer/themes.js、test/context-compaction.test.js、test/ctxwin.test.js、.claude-ui/run-tests-managed.js(新)。
- 注意:改动生效需**重启 Drafter 进程**(环境变量与渲染层都在进程启动时装载)。

## v0.15.17 附加(2026-09-22,已发布—随 v0.15.18 一并发出)— 打包面收敛 / perMachine 对齐 / 签名预留

> 与上面「上下文压缩阈值」小节同属 v0.15.17,由并行会话独立完成,互不覆盖。用户提出:「清掉 / 收敛 asarUnpack / 统一 perMachine / 上代码签名」。本小节的打包配置**对已装旧版无效**,从 v0.15.18 的安装包起才生效(实测 v0.15.18 安装包 177,359,427 字节,散文件 214 个)。

- **根因(只读取证)**:每次装新版后整机卡顿 = 更新时整棵安装树重写 + 首次运行一个全新未签名的大 PE,杀软逐个文件实时扫描阻塞文件系统过滤链,全机 I/O 排队,扫完按哈希缓存后自愈。实测 `C:\Program Files\Drafter` 有 **7,792 个文件 / 673.7 MB**,其中 `resources\app.asar.unpacked` 摊了 **7,714 个散文件**,`Drafter.exe` 单文件 **200.4 MB 且未签名**;本机活跃杀软是**火绒**(HipsDaemon/usysdiag/wsctrl 在跑,Defender 实时保护已被接管为 `False`),排除列表为空。已排除:内存、磁盘、Defender 双扫、应用自身启动重活、多实例。已清掉 `%LOCALAPPDATA%\drafter-updater\pending\`(释放 174.4 MB);根目录 `installer.exe`/`current.blockmap` 是差分更新基线,**保留不动**。
- **判定性实验(决定走 A 还是 B,结论:A)**:临时目录造 `app.asar`(含 `package.json`+ESM+CJS+嵌套 `node_modules`),用本仓 Electron 38.8.6 / Node 22.22.0 起最小主进程实测——从 asar 内 `fs.readFileSync` ✅、`fs.readdirSync` ✅(含目录)、`await import(pathToFileURL(...))` ✅(**相对说明符与经 asar 内 `node_modules` 解析的 bare specifier 均通过**)、CJS `require` ✅;向 asar 内写文件 ❌ ENOENT(只读)。⇒ `src/main/harness/harness-bridge.js` 原注释「ESM import 读不到 asar 虚拟文件系统」**被实验证伪**,`asarUnpack` 可以大幅收敛。
- **收敛 asarUnpack**:`package.json` 的 `build.asarUnpack` 由 7 条 vendor glob(`**/lib/**`、`apps/web/dist/**`、`**/cordis.patch.yml`、`**/package.json`、`vendor-deps/**` 等)收敛为 4 条:`**/*.node`、`**/*.dll`、`**/*.exe`、`vendor/deepseek-harness/apps/cli/config/**`(后者须真实目录,`skill-filesystem` 预设依赖)。**散文件 7,713 → 214**,这 214 个全部来自 electron-builder smartUnpack 的整包解包行为(含原生文件的包整体落地):`node-pty` 179 + `vendor-deps/node-pty` 18 + `apps/cli/config` 10 + `@img/sharp-win32-x64` 3 + `claude-agent-sdk-win32-x64` 3 + `@koromix/koffi-win32-x64` 1。安装包 **182,834,756 → 177,360,107 字节**(-5.5 MB),`app.asar` 65.4 MB。
- **加载路径跟随**:`harness-bridge.js` 的 `resolveHarnessRoot()` 打包态不再 `.replace('app.asar','app.asar.unpacked')`,直接返回 asar 内路径(附注释记录被证伪的旧假设与实验结论);ESM resolve hook 与 CJS `Module._resolveFilename` 索引基于 `HARNESS_ROOT` 自动跟随,无需改。
- **修「应用每次启动往安装目录写文件」**:`renderHarnessIndex()` 原先把生成的 `index.electron.html` 写进 `HARNESS_ROOT/apps/web/dist/`(即 **C:\Program Files 安装目录**,连函数自身注释都写的是「写到 userData/harness-web/index.html」,名实不符)。现改为写 `path.join(app.getPath('userData'), 'harness-web', 'index.html')`,并把 `href="/`、`src="/`(含 `/plugins/<id>/client.js` 分支)改写为指向 `HARNESS_ROOT/apps/web/dist/` 的**绝对 `file://` URL**。这是 per-machine 安装的脆弱点,也是 `apps/web/dist/**` 曾经必须解包且可写的原因。`build.files` 里的 `!vendor/deepseek-harness/apps/web/dist/index.electron.html` 保留但已是历史遗留。`test/packaged-smoke.js` 的页面匹配同步改为 `harness-web/index.html`(src/renderer/harness.js 与 preload.js 的注释一并更新)。
- **perMachine 对齐**:`build.nsis` 加 `perMachine: true`、`allowToChangeInstallationDirectory: true → false`,与当前实际安装形态(per-machine,`C:\Program Files\Drafter`)对齐并锁死,避免再分叉出 per-user 副本。只影响**新安装**;已装机器路径不变,electron-updater 就地更新(代价:每次更新弹 UAC)。
- **代码签名预留**:仓库不写任何签名字段,electron-builder 原生读 `CSC_LINK` / `CSC_KEY_PASSWORD`,未配置时打印 `no signing info identified, signing is skipped` 且构建照常成功——这就是「预留」。新增 `build/signature-status.js`(`Get-AuthenticodeSignature` 体检,固定 UTF-8 输出防中文机构名乱码);`build/verify-package.js` 输出增加 `unpackedFiles`/`signed`(可设 `DRAFTER_REQUIRE_SIGNING=1` 让未签名直接非零退出,留给正式发版/CI);`.claude-ui/release-0.15.16.js` 校验后加一行签名打印(后续发版脚本从此模板继承);新增 `docs/code-signing.md`(OV/EV 选择、`CSC_LINK` base64 .pfx 用法、**证书到位产出 `signed: Valid` 之前不得填 `win.publisherName`**——否则 NSIS 更新发布者校验必失败、以及真正止住卡顿要加火绒排除目录)。
- **回归守卫**:`build/verify-package.js` 新增断言——解包面出现白名单外的散文件即失败(白名单 = 原生/二进制扩展名 ∪ `apps/cli/config` ∪ 「自身含原生文件的包目录」),且解包文件数 ≤ 400。
- **验证**:`npm test` **422/422**;`npm run build` 成功;`npx electron-builder --dir` 成功;**打包态 CDP 冒烟** `node test/packaged-smoke.js dist` **PASS**(`harnessUi:true`、`terminal:true`,截图确认 harness 界面渲染,`Debug cleanup {"ok":true,"count":1,"pending":[]}`);`node build/verify-package.js dist/verify-0.15.16` → `{version:0.15.16, matchingSourceFiles:101, claudeBinary:true, unpackedFiles:214, installerBytes:177360107, sha512Verified:true, signed:"NotSigned"}` 退出 0,`DRAFTER_REQUIRE_SIGNING=1` 退出 1(符合预期);签名助手单独验证:本仓构建 `NotSigned`,系统 `notepad.exe` `Valid`(Microsoft 主体)。为不覆盖已发布的 v0.15.16 产物,完整安装包构建到 `dist/verify-0.15.16`。
- **调试偏差记录**:受管入口(debug-runtime launch)三次跑打包态冒烟均命中本项目既定坑——supervisor(powershell)先于目标死亡、写不出 `released`,其 Job 句柄关闭触发 `KILL_ON_JOB_CLOSE` 把目标进程树无异常硬杀,记录卡 `running`;`Start-Sleep` 保活无效。按既定应对:`node .claude-ui/reconcile-scope.js <scope>` 复核 PID 全死后改写 released → `cleanup`/`verify` 均 `{"ok":true,"count":3,"pending":[]}`;**冒烟改隔离直跑**(`env -u ELECTRON_RUN_AS_NODE` 直起 electron)+ finally 自清理。收尾核对:无 `dist/win-unpacked` 残留进程、CDP 端口已释放、临时 userData 已删;用户已安装的 6 个 `C:\Program Files\Drafter\Drafter.exe`(2026-09-18 创建)**未触碰**。
- 文件:package.json(build.asarUnpack/build.nsis)、src/main/harness/harness-bridge.js、build/verify-package.js、build/signature-status.js(新)、docs/code-signing.md(新)、test/packaged-smoke.js、src/renderer/harness.js、preload.js、.claude-ui/release-0.15.16.js。
- 注意:打包配置改动**对已安装的旧版本无效**,需随下一次发版出包才生效;**签名与火绒排除都不是代码能解决的**,真正止住卡顿仍需在目标机器上把 `C:\Program Files\Drafter` 与 `%LOCALAPPDATA%\drafter-updater` 加入火绒排除目录,本轮只是缩小了每次更新被扫描的文件面。

## v0.15.16(2026-09-18,已发布)
- 发布:GitHub Release [v0.15.16](https://github.com/891458249/Drafter/releases/tag/v0.15.16)(Release ID 391389973)已发布并设为 Latest;exe(182,834,756 字节)/blockmap(189,518 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.15 → latest 0.15.16, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.16.js` 已入库,本次上传一次通过(无 ECONNRESET)。产物 `dist/release-0.15.16/`。commit `38ec21a8`(功能+版本 bump),tag `v0.15.16`。
- 新功能:**「扩展」板块**(顶栏 Chat 与「创作」之间)——Skill 技能与自定义子 Agent 的创建/管理,存储走 Drafter 内部 store(`settings.extensions`,与 gems 同模式),不写 `~/.claude` 文件。
  - 板块界面(src/renderer/extensions.js + index.html `#ext-root`/`#ext-modal`):页签「技能 | 子 Agent」+ 卡片网格(我的/预置模板分组,卡片含启停、编辑、导出、删除/复制副本);编辑器复用 Gem 三栏弹窗(列表 | 表单 | 预览),`app.js` 注册 `SECTIONS`/`NON_SESSION_SECTIONS` + `enterSection` 骨架(同素材板块)。
  - **Skill 生效 = 渐进式披露(原生做法)+ 手动指定优先**:会话挂载(`meta.skillIds`,composer「🧩 技能」菜单第一级)后,systemPrompt 只放「名称+描述」索引(`buildSkillsIndex`,`<drafter-skills>` 块);模型需要时调用进程内 MCP 工具 `use_skill`(SDK `createSdkMcpServer`+`tool`,注册于 `drafter_skills` server)取回完整指令;发送前可在菜单第二级「本条消息指定」(★,pinSkillIds)手动勾选,**全文随该条消息注入(`composePinnedSkills`,`<drafter-skill>` 块,总量截断 8000),优先级最高**,发送后自动清空;极速问答(零工具)无 MCP,但 pinned 注入天然兼容。索引中对 pinned 技能标注「优先级最高,同类需求不再调用其他技能」。
  - **子 Agent 生效 = 按作用域挂载**:全局(所有 code/chat 会话)/指定项目/指定会话(创建时绑定,或留空在 composer「🤖 子 Agent」菜单底部「自定义 Agent」分组按需挂载,写 `meta.customAgentIds`);`buildCustomAgents` 过滤后合并进 SDK `options.agents`(名称冲突加后缀去重),agent 定义带 `disallowedTools:['Agent','Task','Workflow','SendMessage']` 防嵌套委派;固定模型(`keyId|model`,表单下拉按启用 Key 的 chat 模型列出)登记委派守卫强制钉模型,未固定(跟随会话默认)的登记 `null` 放行并剥掉 model 覆写。
  - **AI 辅助起草**:编辑器「✨ AI 起草」行内输入一句目标描述(window.prompt 在本应用不可用,v0.15.13 教训),`ext:draft` 经 chat/completions(20s 超时,复用 gems:rewrite 链路)按「何时触发/步骤/输出格式」(技能)或「角色/职责边界/汇报方式」(Agent)生成正文。
  - **内置模板**:3 技能(代码审查清单/Commit 信息助手/文档撰写)+ 3 子 Agent(code-reviewer/test-engineer/code-explorer,带工具白名单),`preset:true` 只读,可复制副本后编辑;`seedPresets()` 启动播种,已存在跳过。
  - **导入/导出**:Claude Code 原生 frontmatter .md 格式(`serializeMd`/`parseMd`,技能参考文件以 `## 参考文件:name` 分节;导入的 Agent model 强制置 null 防泄漏本机 keyId);对话框在主进程(ext:export/ext:import),导入先入编辑器确认后才落库。
  - 主进程:新模块 `src/main/extensions.js`(CRUD+截断上限 60/200/30000+seedPresets+渐进披露三件套+buildCustomAgents+serializeMd/parseMd+buildDraftPrompt,不依赖 electron 便于单测);main.js 六个 IPC(ext:list/save/remove/draft/export/import)+ `sess:send` 支持 `pinSkillIds`(过滤到已挂载子集,pinned 块拼到 SDK 输入前、历史回显用原文)+ `sess:setSkills`/`sess:setCustomAgents`(仅 code/chat,needRestart 语义同 setGem);sessions.js `buildSessionAgents` 合并自定义 Agent + 委派守卫 null 模型分支 + start() 技能索引注入与 use_skill MCP 注册(仅 Agent 模式);`create()`/`branch()` 支持 `skillIds`/`customAgentIds` 继承;`setSkills`/`setCustomAgents` 方法(busy 时 pending 重启生效)。
  - 渲染层:composer 新增「🧩 技能」选择器(两级菜单:会话挂载持久化 + 本条 ★ 指定发送后清空,芯片计数 `技能 (n+m★)`);agents-ui.js 菜单底部「自定义 Agent」分组(会话作用域可勾选挂载,global/project 显示自动生效);input.js 发送时 `consumePins()` 透传;chat.js 顶栏刷新同步技能选择器;styles.css sec-ext 区块与 ext-* 样式。
  - 验证:`npm test` **421/421**(新增 test/extensions.test.js 18 例:CRUD/preset 拒改拒删/seed 幂等/mountedSkills 排序+停用跳过/buildSkillsIndex pinned 标注/composePinnedSkills 截断/useSkill 三分支/buildCustomAgents 作用域过滤+customAgentIds+null 模型守卫/去重后缀/slugify CJK 回退/serializeMd↔parseMd 往返/buildDraftPrompt);端到端探针 `.claude-ui/probe-ext.js`(隔离 userData+CDP+本地假 OpenAI 服务当真 Key,真实 SDK 发送经 oai-proxy 翻译层):扩展板块模板渲染 → 模板复制副本 → AI 起草回填 → 保存 → composer 挂载+★指定 → **真实发送后假网关抓到 `<drafter-skills>` 索引(含技能名)与 `<drafter-skill>` pinned 全文(含标记)+ 用户原文** → pin 发送后清空 → 自定义 Agent(session 作用域不绑定)经子 Agent 菜单挂载 → `meta.skillIds`/`meta.customAgentIds`/`settings.extensions` 全部落库,10 步全 PASS。
  - 调试偏差记录:探针按 v0.15.14 既定应对「隔离直跑 + finally 自清理」(受管 launch 会 KILL_ON_JOB_CLOSE 硬杀长探针);跑完核对 CDP 端口已关、无残留 electron 进程、临时目录已删,debug-runtime cleanup/verify 均 `ok:true,count:0`。
  - 文件:src/main/extensions.js(新)、test/extensions.test.js(新)、main.js、preload.js、src/main/sessions.js、src/index.html、src/styles.css、src/renderer/extensions.js(新)、app.js、input.js、chat.js、agents-ui.js、.claude-ui/probe-ext.js(新)。

## v0.15.15(2026-09-17,已发布)
- 发布:GitHub Release [v0.15.15](https://github.com/891458249/Drafter/releases/tag/v0.15.15)(Release ID 390598679)已发布并设为 Latest;exe(182,822,106 字节)/blockmap(189,216 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.14 → latest 0.15.15, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.15.js` 已入库,本次上传一次通过(无 ECONNRESET)。产物 `dist/release-0.15.15/`。commit `edbbfd75`(本会话 net-error 透传 + 并行会话帧差抽帧/弹窗误关)+`8cb7273d`(版本 bump),tag `v0.15.15`。
- 修复(生产落地):配置的视频/音频辅助模型失败时附件分析直接落空——典型场景:Kuro key 当日额度耗尽(HTTP 429「API Key 额度已用完」),视频只注入文件名/大小元信息,主模型回答「不能直接解析 MP4 画面内容」。
  - 根因(实测确认):aux-models.js `resolveMediaRef` 只调用配置的「key|模型」一次,失败后无重试、无替代通道,直接 `metaFallbackText`;与用户配置的 Kuro key(…2f6a)当天 429 叠加,视频解析功能整体失效。
  - 修复:**跨 Key/模型自动兜底**(src/main/aux-models.js)。配置的辅助分析失败(3D 除外,本就没有 chat 二进制入参)且调用方提供 `listKeys` 时:枚举其余启用的 Key(≤2 个)→ GET `/v1/models`(15s 超时)→ `rankVisionCandidates` 过滤明显非多模态模型(embed/whisper/tts/dall/rerank 等),名称带视觉线索(vl/vision/omni/4o/gpt-5/gemini/claude/kimi/k3/qwen/glm 等)的排前 → 逐个尝试(每 key ≤3 个模型,单次超时收紧到 45s,避免发送流程长时间卡住)→ 任一成功即返回 `<附件分析>`。全链失败才落回元信息兜底,失败原因聚合各 key 的错误(如 `Kimi/k3:HTTP 500 …`)写进兜底文本供排查。
  - 视频抽帧器整条链只跑一次(惰性 `getVideoFrameExtractor` 外包一层缓存),各候选复用同一份帧,不重复解码;`analyzeMedia` 新增 `opts.timeoutMs` 透传 fetchJson。
  - 接线:main.js `sess:send` 的 injectMedia 调用补传 `listKeys`(枚举 store 里启用的 apiKeys);`injectMedia`/`resolveMediaRef` 签名扩展,旧调用方不传 listKeys 时行为与之前完全一致(单测锁定)。
  - 状态提示:兜底时 composer 显示「配置的辅助模型失败,正在尝试 <Key 名> 的模型…」;全失败显示「已注入文件信息兜底」。
  - **隐私提示**:兜底会把附件内容(视频关键帧/音频/图片)发往用户配置的其他提供方——这是用户明确要求的「不管用什么模型都要能解析」行为;不想让附件离开某提供方时,可在 API Keys 设置里禁用其余 Key(兜底只枚举 `enabled !== false` 的)。
  - 验证:`npm test` **388/388**(新增 5 例:429→兜底成功且调用序为「主 key chat→兜底 key models→兜底 key chat」、Bearer 换兜底 key、状态提示含 key 名;全失败聚合 `Kimi/k3` 错误;不传 listKeys 不触发兜底;3D 不发任何请求;rankVisionCandidates 排序)。端到端探针 `.claude-ui/probe-extract-e2e.js`(真实 store:Kuro …2f6a 已 429 + Kimi …TwHl 可用,真 79.5s mp4):场景抽帧 6 帧 → 主配置 429 → **自动兜底 Kimi k3 返回完整中文视频描述**(跑酷动画,镜头/角色/场景逐帧还原),injectMedia 全程 55.8s,`block0 ANALYSIS-OK`。
  - 另证:Kimi key(api.kimi.com/coding/v1)图像通道实测可用——有效 PNG 经 image_url 块发送,k3 与 kimi-for-coding 均正确回答「红色」(HTTP 200)。
  - 文件:src/main/aux-models.js(listModels/rankVisionCandidates/跨 Key 兜底/timeoutMs)、main.js(listKeys 接线)、test/aux-models.test.js(+5 例)、.claude-ui/probe-extract-e2e.js(step2 改走生产 injectMedia 全链)。
  - 关联:并行调试会话 s_d5419630-265 同时排查了同一用户问题(怀疑 injectMedia 未接线——实际 main.js:790 正常调用,真正原因是 Kuro 429),结论经 .claude-ui/memory.md 对齐,未产生冲突改动。
- 增强(应用户要求,会话 s_d5419630-265):**禁用均匀抽帧,选点全部由前后帧对比驱动**(src/main/video-frames.js)。
  - 改动:①缩略扫描阶段除场景边界外,新增记录每个采样点的**帧差样本** `{t, diff, luma}`(diff=与前一点的缩略图帧差,0~255),扫描中途异常不再清空已采集结果;②`pickFrameTimes` 删除 `(i+0.5)/n` 均匀公式路径——无场景边界时不再造内容无关的中点,直接按帧差降序贪心取点(最小间隔 `max(0.5, d/(n*3))` 防扎堆、黑帧 luma<8 排除);场景不足时先帧差补齐,**仅在完全无帧差数据(扫描失败)时才用最大空隙二分兜底**;分层取(场景过多)时段内无场景点的兜底由「段中心」改为「段内帧差最高点」;③Node 侧与页内注入副本(`pickFrameTimesInPage`)同步改,extractInPage 把 samples 传入选点。
  - 验证:`npm test` **393/393**(test/video-frames-pick.test.js 由 15 例增至 20 例:删均匀采样回归用例,新增帧差贪心取点/黑帧样本排除/无数据二分兜底/帧差优先于二分/最小间隔防扎堆/分层空段帧差兜底);端到端探针 `.claude-ui/probe-extract-e2e.js`(真 Kimi k3 + 79.5s 跑酷视频):抽帧 6 帧(36 场景,1806ms)→ **injectMedia 生产链 Kimi k3 返回完整中文视频描述**(场景/角色/动作逐帧还原,59.2s,`block0 ANALYSIS-OK`)。注:该视频场景多(36>6),实际走分层取场景中点路径,与 v0.15.14 选点一致;帧差驱动路径由单测覆盖。
  - 文件:src/main/video-frames.js、test/video-frames-pick.test.js。
- 修复(应用户要求,会话 s_d5419630-265):**弹窗(设置面板等所有 .modal-mask)误关**——面板内按下鼠标(拖选文字/拖滑块)移到蒙层空白弹起,或蒙层按下移到面板内弹起,面板都会被关掉。
  - 根因:src/renderer/app.js 蒙层关闭用 `click` 事件;按下与弹起落在不同元素时,浏览器把 click 派发到最近公共祖先(即蒙层),`e.target === mask` 成立 → 误关。
  - 修复:改为分别跟踪 `mousedown`/`mouseup` 落点,**两次都落在蒙层空白上(一次完整点击)才关闭**;任一落在面板内则不关。Esc 关闭不变。
  - 验证:探针 `.claude-ui/probe-modal-click.js`(新;CDP `Input.dispatchMouseEvent` 走真实输入管线——合成 Event 复现不了 click 的公共祖先派发):A 面板内按下→蒙层弹起=不关、B 蒙层按下→面板内弹起=不关、C 蒙层完整点击=关闭、D Esc=关闭,全 PASS;`npm test` 393/393。
  - 文件:src/renderer/app.js、.claude-ui/probe-modal-click.js(新)。
- 修复:**OpenAI 协议 Key 的网关连接失败时错误只剩「504 上游请求失败:fetch failed」**,无从分辨网关故障/域名被阻断/DNS 异常——典型场景:temp key(xhspeed.xyz)的 GPT 6-ASTRA 会话报「API Error: 504 上游请求失败:fetch failed…check your inference gateway (127.0.0.1:6405)」。
  - 根因(探针坐实,两轮):xhspeed.xyz 在当前网络被 **SNI 定点阻断**——DNS 正常(Cloudflare 104.21.3.46),TCP 443 可连(225ms),但 TLS ClientHello 携带 SNI=xhspeed.xyz 即于 ~240ms 内被中间盒 RST;同 IP 换 SNI=www.cloudflare.com 握手成功(证书有效);DoH(1.1.1.1)解析出的另一 IP 172.67.130.60 同样 RST;HTTP/80 Host 头被注入 403。与 Key 本体、认证头(x-api-key/Bearer 均试)、协议翻译均无关;对照 Kuro 网关 25ms 正常。截图中的 176.5s 是 claude.exe 对 5xx 静默退避重试的累计时长,单次底层失败仅 ~0.3s。
  - 应用侧缺陷:Node fetch 把真因藏在 `e.cause`(TypeError「fetch failed」只是外壳),oai-proxy/model-guard/keys/aux-models/llmtext 均只透传 `e.message`,用户看不到 ECONNRESET 等关键信息。
  - 修复:新增 `src/main/net-error.js`(`netErrorText(e)`:展开 cause 链 + 按 code 给人话提示——ECONNRESET→「连接被重置:该网关域名可能被当前网络阻断,或网关正在拒连」、ETIMEDOUT/UND_ERR_CONNECT_TIMEOUT→「连接超时」、ENOTFOUND→「DNS 解析失败」、ECONNREFUSED→「连接被拒绝」、证书类→「TLS 证书校验失败」);oai-proxy 的 504 带上游 host:`上游请求失败(xhspeed.xyz):fetch failed(read ECONNRESET) —— 连接被重置:…`;model-guard 直连网关连接失败同样翻译且不再误报「内部错误」;keys.js 刷新模型/余额、aux-models.js 附件辅助分析、llmtext.js 文本补全的裸 `e.message` 全部替换。
  - 验证:`npm test` **403/403**(新增 test/net-error.test.js 7 例 + 3 个集成例:oai-proxy 指死端口 504 含 ECONNREFUSED/host/提示、keys refreshModels 连接失败含 cause、model-guard 直连失败不误报内部错误);端到端探针 `.claude-ui/probe-proxy-e2e.js`(复制真实 store + 真实被阻断域名走修复后代理):**310ms 返回 504「上游请求失败(xhspeed.xyz):fetch failed(read ECONNRESET) —— 连接被重置:该网关域名可能被当前网络阻断…」**。诊断探针 probe-xhspeed.js(DNS/TCP/TLS/GET models/POST chat×两种认证头/对照 Kuro)、probe-xhspeed2.js(同 IP 异 SNI/DoH 异 IP/HTTP 80 对照)。
  - 文件:src/main/net-error.js(新)、oai-proxy.js、model-guard-proxy.js、keys.js、aux-models.js、llmtext.js、test/net-error.test.js(新)、test/oai-proxy.test.js、test/keys.test.js、test/model-guard-proxy.test.js、.claude-ui/probe-xhspeed.js、probe-xhspeed2.js、probe-proxy-e2e.js(均新)。
  - 用户侧结论:temp Key 本体与应用配置均无问题;**xhspeed.xyz 被当前网络按域名定点阻断**,须换网络(如手机热点)/开启代理/向该中转站索取备用入口域名才能恢复使用。本机当前无系统代理、无常见本地代理端口监听。
  - 调试偏差记录:首个探针经受管 launch 启动后,目标进程在 TCP 阶段被 KILL_ON_JOB_CLOSE 硬杀(supervisor 先死,本机既有问题,见 v0.15.14 坑记录),已用 reconcile-scope.js + cleanup + verify 收尾(ok:true);后续探针按既定应对改「隔离直跑 + 看门狗自清理 + 进程核对」。

## v0.15.14(2026-09-16,已发布)
- 发布:GitHub Release [v0.15.14](https://github.com/891458249/Drafter/releases/tag/v0.15.14)(Release ID 389937592)已发布并设为 Latest;exe(182,819,467 字节)/blockmap(189,459 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.12 → latest 0.15.14, hasUpdate:true`(覆盖 v0.15.13 未发布区间)。发布脚本 `.claude-ui/release-0.15.14.js` 已入库;首次跑 exe 上传完成后 blockmap 阶段 ECONNRESET(exe 资产 SHA-256 已验证),重跑断点续传完成全部校验。产物 `dist/release-0.15.14/`。commit `5c9969a5`(场景检测)+`32f95727`(v0.15.13 重命名修复,随本版一并发布)+`68083396`(版本 bump),tag `v0.15.14`。
- 增强:视频抽帧从「所有视频固定均匀抽 6 帧」升级为**场景切换检测抽帧**——在镜头边界处取帧,模型能看到每个场景的代表画面。
  - 流程(src/main/video-frames.js,仍在同一隐藏 offscreen 窗口内):①**缩略扫描**——按时长定步长 `clamp(duration/48, 0.5s, 2.5s)`、≤48 个扫描点,每点 seek 后缩绘 64×36 取像素,与上一点算平均绝对亮度差(Rec.601 权重 1.0)+每通道 RGB 平均绝对差(权重 0.33),`diff>26` 记一次切换(硬切>35、淡入淡出 8~20),边界取两点中点;全黑帧(亮度<8)不计切换;扫描整体异常时静默回退无场景信息。②**选点**——候选=各场景区间中点;候选过多时按时长等分 n 段取距段中心最近者,不足时全保留并在最长时间空隙递归二分补齐;首场景点距 0 <1s 固定保留(封面/标题帧);**无场景信息时退化为旧版均匀采样 `(i+0.5)/n`**(修复了「单候选+无边界」误入补齐路径的退化 bug,Node 与页内两份实现同步修)。③**高清取图**不变(≤768px、JPEG q0.72,单点失败跳过)。
  - **时长分档帧数**(替代固定 6):<30s→4、<180s→6、<600s→8、否则→10;`extractVideoFrames(filePath,{frames})` 显式传入仍优先(测试/探针用),默认 0=auto 走分档。`EXTRACT_TIMEOUT_MS` 60s→90s(扫描增加 seek 次数,实测每点 30~80ms)。
  - 返回结构扩展 `{scenes, scanned}` 诊断字段;src/main/aux-models.js 视频 prompt 改为「按时间顺序在场景切换处抽取的 N 个关键帧(含镜头边界,共检测到约 K 个镜头)」,告知模型帧间时长不均匀。
  - 可测性:场景判定/选点/分档抽成纯函数 `isSceneCut`/`pickFrameTimes`/`framesForDuration` 导出;页内副本以源码注入同一 IIFE(函数体内裸引用经闭包解析),文件内注释标明「改任一侧须同步另一侧」。
  - 验证:`npm test` **383/383**(新增 test/video-frames-pick.test.js 15 例:分档边界、黑帧/渐变/硬切判定、无边界均匀回归、首帧保护、空隙二分补齐、非法入参);端到端探针 `.claude-ui/probe-extract-e2e.js`(真 key + 10.9MB/79.5s/720p 跑酷视频):**扫描 48 点检出 36 个场景,取 6 帧 t=[0.8,7.5,24,38.9,55.5,68.7](非等距、首帧保留),全程 1781ms**(<10s 预算);analyzeMedia 链路到达网关,因该 key 当日额度用尽返回 HTTP 429(外部原因,非代码缺陷;图像通道完整描述已在 v0.15.12 验证,本次改动不影响请求结构)。
  - 坑(debug-runtime 受管启动,已坐实根因):本机 supervisor(powershell)总是先于目标死亡且不写 `status:'released'`;supervisor 一死,其持有的 Job 句柄关闭触发 **KILL_ON_JOB_CLOSE**,目标进程树被无异常硬杀——表现为「日志停在中途、无任何错误、进程消失」。launch 任务先结束时的控制台拆除只是死因之一,任务存活期内 supervisor 同样会死(疑似 EDR/环境策略)。应对:①死记录用 `.claude-ui/reconcile-scope.js <scope>`(参数化版,rt.identity 复核 PID+启动时间全死后改写 released)再 cleanup/verify;②长探针改「隔离直跑+自清理+进程核对」并如实记录偏差。探针另加:raw fetch 20s 超时 + Node https 对照行(区分 Chromium/Node 网络栈问题)。
  - 文件:src/main/video-frames.js(重写)、src/main/aux-models.js(prompt)、test/video-frames-pick.test.js(新)、.claude-ui/probe-extract-e2e.js、.claude-ui/reconcile-scope.js(新)、.claude-ui/run-probe-managed.sh(新,launch+等待+cleanup 单任务保活脚本)。

## v0.15.13(2026-09-16,已发布,随 v0.15.14 一并发出)
- 修复:会话右键菜单「重命名」点击无任何反应。
  - 根因:src/renderer/sessions-ui.js `renameSession` 用 `window.prompt()` 收集新名,而本应用 Electron 渲染进程不实现 prompt 对话框——**静默返回 null**,于是点击后什么都不发生(项目组 `.proj-name` 双击重命名同病,同样用 prompt)。
  - 修复:改为**行内 input 重命名**(同 canvas.js renameCanvas 既有模式):右键点「重命名」后,会话项标题就地替换为输入框(预填现名、自动聚焦全选),Enter 提交调 `api.sessRename`,Esc/失焦取消或提交;`blur` 与 `Enter` 会相继触发,用 done 哨兵保证只提交一次。项目组名双击重命名同步改为行内 input(`api.projRename`)。
  - 样式:src/styles.css 新增 `.session-item > .sess-rename, .proj-head > .proj-rename` 宽度规则(width:100%,防止输入框撑破侧栏)。
  - 验证:`npm test` **368/368**;端到端冒烟 `.claude-ui/probe-rename-session.js`(dev Electron + 干净 userData,CDP 真实派发):右键会话项 → ctx-menu 四项齐全 → 点「重命名」行内 input 出现且聚焦、菜单关闭 → 改名 Enter 后 store 标题与 DOM 同步更新 → 再次重命名按 Esc 取消标题不变。PASS。
  - 坑(探针):①`api.sessCreate({title})` 不落 title,须创建后 `api.sessRename`;②探针直调 api.sessCreate/sessRename 不触发任何渲染事件,侧栏 DOM 不刷新,须派生 `#session-filter` 的 input 事件强制 `refreshList`;③debug-runtime 受管启动拿不到子进程 stdout,探针结果默认写 `%TEMP%/drafter-rename-probe-<pid>.log`。
  - 文件:src/renderer/sessions-ui.js、src/styles.css、.claude-ui/probe-rename-session.js(新)、.claude-ui/reconcile-debug-scope.js(新,调试 scope 记录清理工具)。
  - 已知同类未修(本次未要求):src/renderer/gems.js:241、src/renderer/canvas2.js:743 仍用 `window.prompt()`,在 Electron 渲染进程同样静默返回 null,相关入口点击无反应;待后续按需修复。

## v0.15.12(2026-09-16,已发布)
- 发布:GitHub Release [v0.15.12](https://github.com/891458249/Drafter/releases/tag/v0.15.12)(Release ID 389682348)已发布并设为 Latest;exe(182,816,459 字节)/blockmap(189,248 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.11 → latest 0.15.12, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.12.js` 已入库;首次跑在发布后公开下载校验阶段 ECONNRESET(资产已全部上传),`--verify` 重跑完成全部校验。产物 `dist/release-0.15.12/`。commit `f9f8e799`,tag `v0.15.12`。
- 修复:配置了 gpt-6-astra 等有视频能力的模型,视频附件仍分析失败(HTTP 400 `Invalid value: 'video_url'`)。
  - 根因(实测确认):Kuro 等 OpenAI Responses 风格网关的上游按 Responses API schema 校验入参,**没有任何视频字节入参**——`video_url` 判为非法值、`input_file` 仅支持文档类 MIME 拒绝 video/mp4;v0.15.11 的 video_url 直发在这类网关必然 400。
  - 修复(生产落地):改为**本地抽帧走图像通道**——新模块 src/main/video-frames.js 用 Electron 自带 Chromium 的 `<video>`+`<canvas>` 在隐藏 offscreen BrowserWindow 解码本地视频(≤64MB,零新增原生依赖),按时间轴均匀抽 6 帧(避开首尾黑帧,采样点 (i+0.5)/n)、缩放至 ≤768px 宽、JPEG q0.72 导出 base64;aux-models.js 视频分支把这些帧作为多个 `image_url` 块 + 「按时间顺序的关键帧」中文 prompt 发 chat/completions(OpenAI 式视频理解标准做法)。回退链:抽帧→image_url;失败回退 video_url(兼容 Qwen-VL/GLM-4V 原生视频网关);再失败元信息兜底。
  - 媒体源用 Blob 注入(主进程读字节→base64→页面 atob→Blob→createObjectURL),避免 sandbox 窗口 file:// 源被 opaque origin 拦截;窗口 `sandbox:false, webSecurity:false`(仅加载 about:blank,无外部内容),帧解码错误细分 HEVC/AV1 等原因。
  - 验证:`npm test` **368/368**(aux-models 视频 3 例:多 image_url 块注入抽帧器、400 回退 video_url、无抽帧器直发 video_url);端到端冒烟 `.claude-ui/probe-extract-e2e.js`(真 Kuro key + 真 10.9MB mp4):抽帧 4 帧/79.5s/1280x720 用 864ms,analyzeMedia 经图像通道由 gpt-6-astra 返回完整中文视频内容描述(32.3s)。
  - 坑(Electron 探针):①`ELECTRON_RUN_AS_NODE=1` 残留时 electron.exe 当纯 node 跑且 `process.versions.electron` 仍有值,判定须用 `typeof require('electron')==='string'`;②探针须 `app.on('window-all-closed',()=>{})`,否则抽帧隐藏窗口销毁后默认 quit 会在 analyzeMedia 的 fetch 途中无声杀进程(EXIT 0);③抽帧窗口销毁触发的默认退出是探针特有问题,正式 App 有主窗口不受影响。
  - 文件:src/main/video-frames.js(新)、src/main/aux-models.js(视频分支改图像通道+回退链+getVideoFrameExtractor 惰性加载)、test/aux-models.test.js、.claude-ui/probe-extract-e2e.js(新)。

## v0.15.11(2026-09-16,已发布)
- 发布:GitHub Release [v0.15.11](https://github.com/891458249/Drafter/releases/tag/v0.15.11)(Release ID 389634983)已发布并设为 Latest;exe(182,813,935 字节)/blockmap(189,440 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.10 → latest 0.15.11, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.11.js` 已入库;首次上传 23% 处 ECONNRESET,重跑经断点续传(自动清不完整 draft 资产)完成。产物 `dist/release-0.15.11/`。commit `085a8bc5`(修复)+`d122df56`(发布脚本),tag `v0.15.11`。
- 修复:视频附件加入后无法被 AI 识别分析——发送时只注入文件名/大小元信息,AI 实际「看不到」视频内容。
  - 根因:src/main/aux-models.js `analyzeMedia` 对 `mediaKind==='video'` 硬编码拒绝(「暂不支持内容分析」),无论是否配置了「视频辅助」模型都直接走元信息兜底;Qwen-VL/GLM-4V/Gemini-OpenAI-兼容等多模态网关实际支持 chat/completions 的 `video_url` 块。
  - 修复:视频辅助改走 `video_url` 块(`data:video/<mime>;base64,…`,≤20MB,mp4/mov/webm 映射 video/mp4、video/quicktime、video/webm),配中文视频分析 prompt(画面/人物/动作/场景/语音转写);网关不支持该块类型时按 HTTP 错误自动落回元信息兜底。3D 模型仍仅注入元信息(chat 接口无二进制入参)。
  - UI:API Keys 弹窗「辅助模型」区加一行说明(图像/音频/视频走所选多模态模型分析,视频需模型支持 video_url 且 ≤20MB;3D 仅注入文件信息)。
  - 文件:src/main/aux-models.js、test/aux-models.test.js(video 2 例+injectMedia 视频 1 例,原「video/model 都拒绝」改为仅 model 拒绝)、src/index.html(辅助模型说明)。
  - 验证:`npm test` **367/367**。注意:需用户在 API Keys 设置里配置「视频辅助」为一个支持视频的多模态模型才会真正分析视频内容;未配置或网关不支持时仍是元信息兜底。

## v0.15.10(2026-09-15,已发布)
- 发布:GitHub Release [v0.15.10](https://github.com/891458249/Drafter/releases/tag/v0.15.10)(Release ID 388933576)已发布并设为 Latest;exe(182,813,736 字节)/blockmap/latest.yml 三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.9 → latest 0.15.10, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.10.js` 已入库(版本号正则修正为 `0\.15\.10`)。产物 `dist/release-0.15.10/`。commit `be74048f`,tag `v0.15.10`。
- 调整:拆分子任务从 composer 工具栏一次性按钮,改为发送按钮旁的**激活/关闭开关**。
  - 位置:`#btn-split-subtasks` 从 `.composer-toolbar` 移到 `.input-wrap` 内发送按钮左侧,圆形图标按钮(与发送/停止同排)。
  - 行为:点击激活(高亮 `.split-on`)后,用户发出的下一条消息**不直接发送**,而是由 `sendMessage` 拦截交给 `split.maybeSplitOnSend(text)` → 当前会话的 AI 把这条需求拆成多个子任务 → 弹出可编辑确认卡片 → 确认后为每个子任务新建并行 code 会话并行执行。再次点击开关即关闭,恢复正常发送。
  - 拆分失败时仍弹出卡片并显示错误,用户可手动编辑/添加条目后确认,或取消。
  - 文件:src/index.html(按钮移位+改图标)、src/renderer/split.js(改开关态 `armed` + `maybeSplitOnSend` 导出 + 失败兜底弹卡)、src/renderer/input.js(sendMessage 拦截)、src/styles.css(`.split-toggle`/`.split-on` 样式、输入框右 padding 48→80px)、.claude-ui/probe-split-subtasks.js(冒烟改走「激活开关→点发送」路径)。
  - 验证:`npm test` **364/364**;端到端冒烟实测激活开关→发送→真实 HTTP 拆分→可编辑卡片→确认→真实建出 2 个并行 code 会话(标题前缀 `⧉`,继承 cwd/model)。

## v0.15.9(2026-09-15，已发布)
- 新增:拆分子任务。composer 工具栏「⧉ 拆任务」→ 当前会话的 AI 把需求拆成多个子任务 → 弹出可编辑的确认卡片(可改标题/说明、增删条目)→ 确认后为每个子任务新建一个并行 code 会话并行执行。
  - 触发:点「⧉ 拆任务」。需求取自输入框文本;为空时回退取当前会话最近一条用户消息。
  - 拆分:新 IPC `sess:splitSubtasks`(main.js)调 `llmtext.complete`,沿用当前会话的 Key/模型(缺省回退活跃 Key 的首个对话模型),不污染当前会话历史。新模块 `src/main/split-subtasks.js` 宽松解析模型返回(剥 markdown 围栏/首尾散文、兼容 title/name/task 等别名字段、标题截断 60 字、数量上限 12)。
  - 确认卡片:`#split-modal` 可编辑列表(split.js),标题 input + 说明 textarea + 每行删除 + 「＋ 加一条」。
  - 并行执行:`sess:spawnSubtasks` 为每个子任务 `sessions.create` 一个继承当前会话 cwd/项目/模型/Key/权限/推理深度/子 Agent/Gem 的 code 会话,标题前缀 `⧉`,同步入队各自子任务并行跑(不切换用户当前视图)。
  - 验证:解析器单测 8 例;`npm test` **364/364**;端到端冒烟 `.claude-ui/probe-split-subtasks.js`——本地假 OpenAI 服务 + 真 Key,真实 `sess:splitSubtasks` 走通 HTTP 拆分、卡片弹窗可编辑/增删、确认后真实建出 2 个并行 code 会话(继承 cwd/model)。
  - 文件:src/main/split-subtasks.js、src/renderer/split.js、main.js(两个 IPC)、preload.js、src/index.html(按钮+弹窗)、src/styles.css(确认列表样式)、src/renderer/app.js(init)、test/split-subtasks.test.js。

## v0.15.8(2026-09-15，已发布)
- 修复:按住鼠标选择文字时,一选就把上方所有文字(含顶栏/侧栏 UI 标签)一并选中。
  - 根因:全局未设 `user-select`。从消息气泡向上拖拽越过 `#messages` 滚动容器上边界时,Chromium 原生选择把焦点跳到顶栏等可选 UI 元素,导致从顶栏到按下点的所有文字被连带选中。CDP 探针复现:在第 3 条 assistant 消息按下拖到顶栏,选中串包含 `v0.15.7 已就绪/视图/面板/项目/会话` 等 UI 文本,焦点落在 `HEADER.topbar` 的按钮上。
  - 修复:src/styles.css 新增选择约束——UI 骨架(`.topbar/.sidebar/.right-panel/.composer/.msg-role/.msg-nav/.scroll-bottom/.update-chip/.usage-chip/.section-switch/.ctx-menu/.modal-mask/.code-card-head/.thinking/.tool-head/.perm-card/.aigc-card-head/.turn-status/.turn-progress/.composer-foot`)设 `user-select:none`;消息正文 `.bubble`、代码块 `.code-card pre`、输入框/编辑器等文字交互区显式 `user-select:text`。消息气泡外的 UI 不再进入选区,向上越界拖拽只保留消息内容。
  - 验证:`npm test` 356/356;CDP 探针 `.claude-ui/probe-text-selection.js` 对打包版实测——修复前焦点逃逸到顶栏按钮、修复后选区锚点/焦点均停留在按下的消息内。

## v0.15.7(2026-09-14，新增改动未发布)
- 新增：上下文自动压缩，满窗口后不再禁止继续对话——SDK Code/Agent 与极速 Chat 会话在约 80% 自动压缩并在同一会话继续，提供方拒绝超长上下文时自动压缩后恢复。
  - 根因：应用此前未启用 SDK 的自动压缩能力；`system/status` 的 compacting/compact_result 事件被当 `ui_other` 丢弃，压缩边界后旧 usage 仍显示 100%；极速 Chat 的「压缩」按钮被禁用且发送失败后可能永久锁发送。
  - 实现：所有 SDK 会话合并 `settings.autoCompactEnabled:true`+按模型表的保守 `autoCompactWindow`（上限 200k，不抬高提供方硬上限），`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80` 提前压缩；换模型经 `applyFlagSettings` 热更新；压缩状态/失败转 `ui_compacting` UI 状态，边界事件使旧用量失效显示「待统计」；result.errors 透传；手动压缩按钮回合中禁用、发送失败可恢复；极速 Chat 重新启用「压缩」按钮。Harness 侧 provider 同步带已知模型 contextWindow（compaction-basic 默认 auto:true、80% 触发、溢出恢复一次）。
  - 文件：src/main/context-compaction.js、src/main/{sessions.js,harness/keys-bridge.js}、src/renderer/{chat.js,app.js,input.js}、test/{context-compaction.test.js,agent-route-live.test.js}。
  - 验证：`npm test` **356/356**；真实 SDK+本地假网关实测：code/chat 在高用量回合自动压缩、压缩边界真实到达、摘要请求真实发出、同一 sdkSessionId 继续对话；提供方 400「prompt too long」自动压缩恢复；手动 /compact 成功；401 不触发压缩重试、中断后原会话仍可继续。隔离 Electron 冒烟（开发版）通过且调试资源清理 ok。
  - 边界：单条消息超过整个窗口或「可压缩历史不足」(too_few_groups) 时无法压缩，UI 明确报错并保留会话可重试；压缩不突破提供方硬上限。本机全局 CLI 会话未改（其自带自动压缩）。
- 新增：全局调试占用释放，覆盖 Drafter 工具会话及本机用户级助手配置。调试收尾须释放本次资源并验证，避免后台进程/调试连接阻止软件更新。
  - 实现：Windows 私有 Job Object 受管启动（挂起创建、入 Job 再运行、关闭 Job 回收后代）；监督器校验 owner PID+创建时间，正常退出优先、有界强制收尾；按 scope/资源隔离状态，连接 release/verify 回调只在内存登记，不能从状态文件执行任意命令。不按软件名杀进程、不退出用户原实例。
  - 接入：SDK PreToolUse/Stop/SubagentStop/失败提示、interrupt/stop/query finally；Harness 会话提示及停止/取消/错误扩展点；应用退出等待清理、更新安装前拒绝带未解除的已登记占用继续。保留现有只读/模型路由守卫和不加载 user settings 的 Key 隔离策略。
  - 本机：追加 `~/.claude/CLAUDE.md`，安装独立 `~/.claude/debug-resources/`，合并 SessionStart/PreToolUse/PostToolUseFailure/Stop/SessionEnd command hooks，原 settings 按时间戳备份；不修改模型、Key 或网关。
  - 文件：src/main/debug-resources/*、src/main/harness/debug-resources-bridge.js、src/main/{sessions,updater}.js、src/main/harness/harness-bridge.js、main.js、scripts/install-debug-cleanup.js、test/{debug-resources,agent-route-live}.test.js、test/packaged-smoke.js、docs/debug-resource-cleanup.md。
  - 验证：`npm test` **348/348**，`git diff --check` 通过；Windows 实测父子进程、owner 退出兜底、TCP 端口重新绑定、独占文件重命名失败→清理后成功；真实 SDK+本地假网关验证 Stop 和 interrupt 清理。开发版 Electron 隔离 userData 冒烟：主界面/存储 IPC/Markdown/终端/Harness 页面正常，最终报告已登记资源清理 `ok:true`；截图超时失败路径同样清理通过，改主窗口截图后冒烟成功。
  - 尚未验证：独立用户级新会话的 hooks 自动触发（脚本 stdin 与 JSON 结构已验证，当前 Drafter 会话不读 user 设置，需 `/hooks` 或重启）；Harness 真实模型回合中的取消/停止清理，仅验证实际 boot/页面；未构建新安装包、未发布、未安装更新。
  - 边界：自动验证只覆盖受管/登记资源，文件锁须针对软件额外检查；用户仅中断一轮但 owner 仍存活时，本机 Stop 不保证触发，必须由 finally/显式 cleanup/SessionEnd 收尾；不承诺任意第三方软件自动解锁，不支持受管 Job 脱离保留。

## v0.15.7(2026-09-10，已发布)
- GitHub Release：[Drafter v0.15.7](https://github.com/891458249/Drafter/releases/tag/v0.15.7)（Release ID 386101043），安装包、blockmap、latest.yml 已上传并设为 Latest；三个附件的 GitHub SHA-256 与本地一致。发布脚本 `.claude-ui/release-0.15.7.js` 先上传草稿再发布，可安全重试不完整上传；`RELEASE.md` 明确了自动更新必须发布附件，不能只推送代码和标签。
  - 发布验证：公开 `releases/latest/download/latest.yml` 与本地完全一致；公开下载完整安装包和 blockmap 后 SHA-256 校验通过；调用应用实际 `checkRepoVersion()`，返回 `current: 0.15.6, latest: 0.15.7, hasUpdate: true`。未操作用户正在运行的应用，也未自动安装更新。
- 项目检查修复：
  - 只读保护：统一检查 Agent 的 PreToolUse、权限回调与挂起权限卡；阻止 shell/MCP/子 Agent 等无法确定写入范围的调用，保留读取和对其他文件的受控编辑。规范化相对路径、Windows 大小写、目录链接、硬链接及 NTFS 文件流，检查异常时拒绝执行。此保护覆盖 Drafter Agent 调用，不替代操作系统权限或限制外部进程。
  - 配置恢复：配置损坏不再静默退回空数据；有效 `.bak` 可恢复；无可用备份时保留原件、停止保存并显示启动错误。保存采用临时文件、fsync、原子替换和上一版备份，恢复写入前另存损坏原件，读写错误向调用者传递。
  - 网关设置：`agentSettingSources()` 按规范化路径身份比较，覆盖 Windows 大小写及目录链接，补齐本版本原有修复。
  - Markdown：原始 HTML 按文本显示，屏蔽危险链接协议与实体混淆，保留普通 Markdown、表格、代码块和本地文件链接。
  - 构建：移除 tsdown 中的本机绝对路径；新增 `npm run build`/`build:check`，`npm run dist` 自动重建 Harness Web/IPC 产物且明确禁止自动发布。保留仓库中已打补丁的 Harness lib/vendor-deps。
  - 验证工具：增加原子存储、只读边界、Markdown 和真实 SDK 权限回归；提供包内源码比对、安装包哈希校验及独立用户目录的打包态冒烟脚本。
  - 文件：main.js、package.json/package-lock.json、tsdown.config.ts、README.md、RELEASE.md、src/main/{path-identity,projects,sessions,store}.js、src/renderer/{markdown,state}.js、src/index.html、build/{prepare-harness,verify-package}.js、test/{store-recovery,readonly-boundary,markdown,chat-fast,migrations,agent-route-live}.test.js、test/packaged-smoke.js、docs/project-audit-2026-09-10.md、本文件。
  - 验证：`npm test` **337/337**；Harness 构建通过；NSIS 安装包构建通过；87 个应用文件与包内内容一致；claude.exe 解包正确；安装包 SHA-512 与 latest.yml 一致。真实打包态主界面、设置 IPC、Markdown 防注入、node-pty 终端、Harness 引擎与前端均通过冒烟。
  - 产物：`dist/release-0.15.7/Drafter Setup 0.15.7.exe`（182,801,108 字节）、对应 `.blockmap` 与 `latest.yml`。产物使用独立目录，旧版本产物保留。
- 修复:chat 会话切到 Agent 模式必报 `Failed to authenticate. API Error: 403 模型未配置:k3(原始错误:forbidden: model not defined: k3)`,极速模式正常。
  - 根因:chat 会话默认 cwd = 用户主目录;Agent 模式 `settingSources: ['project','local']` 使 claude.exe 把 `<cwd>/.claude/settings.json` 当项目设置加载,而 cwd 为主目录时该文件就是用户级 `~/.claude/settings.json`——其 env 把 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` 钉到 CLI 配置的 Kuro 网关,且 settings env 优先级高于 App 按会话 Key 注入的进程凭据(v0.11.6 排除 `user` 源的同一个坑,换 `project` 入口复活);`--model k3` 仍生效 → 带 Kimi 模型名打到 Kuro → 403。极速模式 `settingSources: []` 不加载文件设置故正常。实测 claude.exe 对 settings 不做向上回溯,仅 cwd == 主目录精确命中。
  - 修复:`sessions.js` 新增 `agentSettingSources(cwd)`——`cwd/.claude` 与 `CLAUDE_CONFIG_DIR` 重合时返回 `[]`(视为无项目设置),否则照旧 `['project','local']`。
  - 文件:src/main/sessions.js、test/chat-fast.test.js(+3 例)
  - 验证:npm test 321/321;用打包版 claude.exe + Kimi 凭据在主目录实测复现原错误、空 setting-sources 恢复正常。
- 工程规范:新增本文件;全局规则「所有改动必须写入版本更新.md」已写入 ~/.claude/CLAUDE.md(供其他 AI 检查者核对)。

## v0.15.6 及更早
- 详见 git 提交历史与 GitHub Releases(本文件自 v0.15.7 起建立)。
