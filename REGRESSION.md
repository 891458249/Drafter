# Claude UI 人工回归清单(B1–B29)

**执行方式**:`npm start` 启动应用后,按下表逐项人工验证。每项测完在「结果」列勾选(☐ → ☑)、填日期,并同步更新 `DEVLOG.md` 中对应项的标注:通过 → `(已回归)`,未测或失败 → 保持/改回 `(未回归)`。

通用前置条件:已配置 API Key(或 claude CLI 登录态可用),`npm start` 能正常进入工作区。

| 编号 | 功能 | 前置条件 | 操作步骤 | 预期结果 | 结果 | 日期 |
|---|---|---|---|---|---|---|
| B1 | 权限确认 UI | 权限模式为 default;会话已启动 | 1. 让 Claude 执行需授权的操作(如运行一条 bash 命令)<br>2. 权限卡弹出后先点「Deny」<br>3. 再次触发后点「Allow once」<br>4. 触发一次 Edit,确认卡片内显示 diff | Deny 时操作被阻止并提示;Allow once 仅放行本次;Edit 卡片显示行内 diff | ☐ 通过 ☐ 失败 | |
| B2 | 中断不销毁会话 | 会话正在输出长回复 | 1. 发送一个需要长输出的 prompt<br>2. 输出中途按 Esc 或点停止<br>3. 再发送一条新消息 | 输出被中断,会话仍存在;新消息在原会话中正常继续 | ☐ 通过 ☐ 失败 | |
| B3 | 会话历史与恢复 | 已有过对话的会话 | 1. 与 Claude 完成至少一轮对话<br>2. 完全退出应用并重启<br>3. 点击该会话 | 历史消息完整重放;继续对话时上下文不丢失 | ☐ 通过 ☐ 失败 | |
| B4 | 多会话并行 | 无 | 1. 新建会话 A 并发起一轮对话<br>2. 侧边栏新建会话 B 发起另一话题<br>3. 在 A/B 间切换 | 两会话独立运行、上下文互不串扰,切换后各自状态完整 | ☐ 通过 ☐ 失败 | |
| B5 | 运行中追加消息 | 会话正在输出 | 1. 发送一个长任务 prompt<br>2. 在输出未结束时再输入并发送一条补充消息 | 补充消息排队注入,不中断当前回合;后续回合能读到该消息 | ☐ 通过 ☐ 失败 | |
| B6 | 权限模式下拉(SDK 全模式) | 无 | 1. 点开顶栏权限模式下拉<br>2. 逐项确认选项<br>3. 切换到 acceptEdits 后让 Claude 改文件 | 下拉含 default/acceptEdits/plan/dontAsk/bypassPermissions(无 Auto);切换后编辑行为符合所选模式 | ☐ 通过 ☐ 失败 | |
| B7 | Diff 视图 | 会话 cwd 是 git 仓库 | 1. 让 Claude 修改若干文件<br>2. 打开「变更」面板<br>3. 逐文件点开 diff | 面板显示 +n -n 指示器;每个文件的 diff 内容与实际改动一致 | ☐ 通过 ☐ 失败 | |
| B8 | Diff 行内评论回传 | 已有 diff 显示 | 1. 在 diff 某行添加评论<br>2. 再加 1-2 条<br>3. 提交评论批量回传 | 评论按文件/行号整理后发回给 Claude,Claude 回复中能看到并响应这些评论 | ☐ 通过 ☐ 失败 | |
| B9 | Review code 按钮 | git 仓库有未提交改动 | 1. 制造若干未提交改动<br>2. 点「Review code」按钮 | 自动发起一轮代码评审对话,内容覆盖当前改动 | ☐ 通过 ☐ 失败 | |
| B10 | PR 监控 | 仓库有 gh CLI 已登录,且当前分支有关联 PR | 1. 推送一个有 PR 的分支<br>2. 在应用中查看 PR 状态区<br>3. 等 checks 出结果(可制造一次失败) | 显示 PR 状态与各 check 结果;失败时出现 Auto-fix 提示并可触发修复对话 | ☐ 通过 ☐ 失败 | |
| B11 | 每会话 worktree 隔离 | git 仓库 | 1. 新建会话时勾选 worktree 隔离<br>2. 在会话中改动文件<br>3. 归档该会话 | 会话在 `.claude-ui-worktrees/` 下独立工作,主工作区不受影响;归档时 worktree 被清理 | ☐ 通过 ☐ 失败 | |
| B12 | @文件引用补全 | 项目内有文件 | 1. 输入框键入 `@`<br>2. 继续输入文件名前缀<br>3. 选中一个文件发送 | 下拉实时过滤文件列表;选中后插入路径,Claude 能读到对应文件 | ☐ 通过 ☐ 失败 | |
| B13 | 图片粘贴/拖拽附件 | 剪贴板有截图 | 1. Ctrl+V 粘贴一张截图<br>2. 再拖一张图片文件进输入区<br>3. 发送并询问图片内容 | 两张图都作为附件显示;Claude 能描述图片内容 | ☐ 通过 ☐ 失败 | |
| B14 | 斜杠命令列表 | 项目 `.claude/commands/` 与 `~/.claude/commands/` 各有至少一个命令 | 1. 输入框键入 `/`<br>2. 浏览/过滤命令列表<br>3. 选一个项目级命令执行 | 内置 + 用户级 + 项目级命令均出现;选中命令正常执行 | ☐ 通过 ☐ 失败 | |
| B15 | Plan 模式审批面板 | 权限模式切到 plan | 1. 提出一个需要多步实现的任务<br>2. 等 Claude 给出计划(ExitPlanMode)<br>3. 在审批卡点「批准」 | 计划以审批卡形式呈现;批准后自动切 acceptEdits 并开始执行计划 | ☐ 通过 ☐ 失败 | |
| B16 | 多面板布局 | 已进入工作区 | 1. 依次打开/关闭 chat、diff、终端、文件、预览、任务面板<br>2. 拖动分栏调整宽度 | 各面板可独立显隐;分栏调整生效且布局不混乱 | ☐ 通过 ☐ 失败 | |
| B17 | 文件编辑器面板 | 项目内有文本文件 | 1. 在 diff 或对话中点击文件路径打开编辑器<br>2. 修改并保存<br>3. 在应用外改动同一文件后再保存 | 文件打开、编辑、保存成功;外部变更后再保存时弹出冲突警告 | ☐ 通过 ☐ 失败 | |
| B18 | 浏览器预览面板 | 有一个可起本地服务的项目 | 1. 打开预览面板访问 `localhost` 页面<br>2. 再尝试打开一个外部网址 | localhost 页面正常渲染;外部站点先弹确认再打开 | ☐ 通过 ☐ 失败 | |
| B19 | 任务面板 | 会话可触发子代理或后台命令 | 1. 让 Claude 跑一个会产生子代理/后台命令的任务<br>2. 打开「任务」面板 | 子代理按 parent_tool_use_id 归组展示;后台任务状态可见 | ☐ 通过 ☐ 失败 | |
| B20 | 视图模式 Normal/Verbose/Summary | 有一段含工具调用的对话 | 1. 依次切换三种视图模式<br>2. 对比同一段对话 | Normal 正常展示;Verbose 展示全部细节;Summary 折叠为摘要 | ☐ 通过 ☐ 失败 | |
| B21 | 用量显示 + /compact | 已完成几轮对话 | 1. 查看顶栏用量 chip<br>2. 点击展开详情<br>3. 点 /compact | 显示 context tokens 与累计费用;/compact 触发上下文压缩并反馈结果 | ☐ 通过 ☐ 失败 | |
| B22 | 快捷键体系 | 已进入工作区 | 1. 按 Ctrl+/ 打开快捷键面板<br>2. 试 Ctrl+N、Ctrl+B、Ctrl+J、Ctrl+1…5<br>3. Esc 关闭弹窗 | 各快捷键行为与面板说明一致;无按键冲突或失灵 | ☐ 通过 ☐ 失败 | |
| B23 | 多终端标签 | 已进入工作区 | 1. 打开终端面板<br>2. 新建第二个终端标签<br>3. 在两个标签分别执行命令 | 两个 pty 独立运行;切换标签输出各自保留;关闭标签互不影响 | ☐ 通过 ☐ 失败 | |
| B24 | Side chat | 主线会话已有对话 | 1. 从当前会话发起 Side chat<br>2. 在 side chat 中讨论分支话题<br>3. 回到主线会话 | Side chat 基于 fork 上下文但不污染主线;主线历史无 side chat 内容 | ☐ 通过 ☐ 失败 | |
| B25 | 会话重命名/归档/筛选 | 有多个会话 | 1. 重命名一个会话<br>2. 归档一个会话<br>3. 用筛选/开关切换归档可见性 | 重命名即时生效;归档会话默认隐藏、可通过筛选找回;重启后状态保持 | ☐ 通过 ☐ 失败 | |
| B26 | OS 通知 | 开两个会话,应用在前台 | 1. 在非当前会话触发一个较长任务<br>2. 停留在当前会话等其完成 | 非当前会话完成时收到系统通知;当前会话完成不打扰 | ☐ 通过 ☐ 失败 | |
| B27 | 定时任务 | 无 | 1. 打开定时任务面板,新建一个 2 分钟后触发的任务<br>2. 等待到点 | 到点自动新建会话并发送设定的 prompt;任务面板记录触发 | ☐ 通过 ☐ 失败 | |
| B28 | MCP servers 管理 UI | `~/.claude.json` 可写 | 1. 打开 MCP 管理面板<br>2. 新增一个测试 server 并保存<br>3. 再删除它 | 新增后写入 `~/.claude.json` 的 mcpServers;删除后配置同步移除 | ☐ 通过 ☐ 失败 | |
| B29 | 自动更新占位(打包配置) | 无 | 1. 检查 package.json 的 build 配置<br>2.(可选)`npm run dist` 打包 | build.win.target 为 nsis;打包产物正常生成;应用内无更新入口(仅占位) | ☐ 通过 ☐ 失败 | |

## 回归执行记录

| 轮次 | 日期 | 执行人 | 通过项 | 失败项 | 备注 |
|---|---|---|---|---|---|
| - | - | - | - | - | 尚未执行首轮人工回归 |
