# Drafter 版本更新记录

> 规则:所有代码改动与更新必须写入本文件(全局规则,见 ~/.claude/CLAUDE.md)。
> 按版本倒序,最新在最上;未发版标注「(未发布)」。

## v0.15.14(2026-09-16,未发布)
- 增强:视频抽帧从「所有视频固定均匀抽 6 帧」升级为**场景切换检测抽帧**——在镜头边界处取帧,模型能看到每个场景的代表画面。
  - 流程(src/main/video-frames.js,仍在同一隐藏 offscreen 窗口内):①**缩略扫描**——按时长定步长 `clamp(duration/48, 0.5s, 2.5s)`、≤48 个扫描点,每点 seek 后缩绘 64×36 取像素,与上一点算平均绝对亮度差(Rec.601 权重 1.0)+每通道 RGB 平均绝对差(权重 0.33),`diff>26` 记一次切换(硬切>35、淡入淡出 8~20),边界取两点中点;全黑帧(亮度<8)不计切换;扫描整体异常时静默回退无场景信息。②**选点**——候选=各场景区间中点;候选过多时按时长等分 n 段取距段中心最近者,不足时全保留并在最长时间空隙递归二分补齐;首场景点距 0 <1s 固定保留(封面/标题帧);**无场景信息时退化为旧版均匀采样 `(i+0.5)/n`**(修复了「单候选+无边界」误入补齐路径的退化 bug,Node 与页内两份实现同步修)。③**高清取图**不变(≤768px、JPEG q0.72,单点失败跳过)。
  - **时长分档帧数**(替代固定 6):<30s→4、<180s→6、<600s→8、否则→10;`extractVideoFrames(filePath,{frames})` 显式传入仍优先(测试/探针用),默认 0=auto 走分档。`EXTRACT_TIMEOUT_MS` 60s→90s(扫描增加 seek 次数,实测每点 30~80ms)。
  - 返回结构扩展 `{scenes, scanned}` 诊断字段;src/main/aux-models.js 视频 prompt 改为「按时间顺序在场景切换处抽取的 N 个关键帧(含镜头边界,共检测到约 K 个镜头)」,告知模型帧间时长不均匀。
  - 可测性:场景判定/选点/分档抽成纯函数 `isSceneCut`/`pickFrameTimes`/`framesForDuration` 导出;页内副本以源码注入同一 IIFE(函数体内裸引用经闭包解析),文件内注释标明「改任一侧须同步另一侧」。
  - 验证:`npm test` **383/383**(新增 test/video-frames-pick.test.js 15 例:分档边界、黑帧/渐变/硬切判定、无边界均匀回归、首帧保护、空隙二分补齐、非法入参);端到端探针 `.claude-ui/probe-extract-e2e.js`(真 key + 10.9MB/79.5s/720p 跑酷视频):**扫描 48 点检出 36 个场景,取 6 帧 t=[0.8,7.5,24,38.9,55.5,68.7](非等距、首帧保留),全程 1781ms**(<10s 预算);analyzeMedia 链路到达网关,因该 key 当日额度用尽返回 HTTP 429(外部原因,非代码缺陷;图像通道完整描述已在 v0.15.12 验证,本次改动不影响请求结构)。
  - 坑(debug-runtime 受管启动,已坐实根因):本机 supervisor(powershell)总是先于目标死亡且不写 `status:'released'`;supervisor 一死,其持有的 Job 句柄关闭触发 **KILL_ON_JOB_CLOSE**,目标进程树被无异常硬杀——表现为「日志停在中途、无任何错误、进程消失」。launch 任务先结束时的控制台拆除只是死因之一,任务存活期内 supervisor 同样会死(疑似 EDR/环境策略)。应对:①死记录用 `.claude-ui/reconcile-scope.js <scope>`(参数化版,rt.identity 复核 PID+启动时间全死后改写 released)再 cleanup/verify;②长探针改「隔离直跑+自清理+进程核对」并如实记录偏差。探针另加:raw fetch 20s 超时 + Node https 对照行(区分 Chromium/Node 网络栈问题)。
  - 文件:src/main/video-frames.js(重写)、src/main/aux-models.js(prompt)、test/video-frames-pick.test.js(新)、.claude-ui/probe-extract-e2e.js、.claude-ui/reconcile-scope.js(新)、.claude-ui/run-probe-managed.sh(新,launch+等待+cleanup 单任务保活脚本)。

## v0.15.13(2026-09-16,未发布)
- 修复:会话右键菜单「重命名」点击无任何反应。
  - 根因:src/renderer/sessions-ui.js `renameSession` 用 `window.prompt()` 收集新名,而本应用 Electron 渲染进程不实现 prompt 对话框——**静默返回 null**,于是点击后什么都不发生(项目组 `.proj-name` 双击重命名同病,同样用 prompt)。
  - 修复:改为**行内 input 重命名**(同 canvas.js renameCanvas 既有模式):右键点「重命名」后,会话项标题就地替换为输入框(预填现名、自动聚焦全选),Enter 提交调 `api.sessRename`,Esc/失焦取消或提交;`blur` 与 `Enter` 会相继触发,用 done 哨兵保证只提交一次。项目组名双击重命名同步改为行内 input(`api.projRename`)。
  - 样式:src/styles.css 新增 `.session-item > .sess-rename, .proj-head > .proj-rename` 宽度规则(width:100%,防止输入框撑破侧栏)。
  - 验证:`npm test` **368/368**;端到端冒烟 `.claude-ui/probe-rename-session.js`(dev Electron + 干净 userData,CDP 真实派发):右键会话项 → ctx-menu 四项齐全 → 点「重命名」行内 input 出现且聚焦、菜单关闭 → 改名 Enter 后 store 标题与 DOM 同步更新 → 再次重命名按 Esc 取消标题不变。PASS。
  - 坑(探针):①`api.sessCreate({title})` 不落 title,须创建后 `api.sessRename`;②探针直调 api.sessCreate/sessRename 不触发任何渲染事件,侧栏 DOM 不刷新,须派生 `#session-filter` 的 input 事件强制 `refreshList`;③debug-runtime 受管启动拿不到子进程 stdout,探针结果默认写 `%TEMP%/drafter-rename-probe-<pid>.log`。
  - 文件:src/renderer/sessions-ui.js、src/styles.css、.claude-ui/probe-rename-session.js(新)、.claude-ui/reconcile-debug-scope.js(新,调试 scope 记录清理工具)。
  - 已知同类未修(本次未要求):src/renderer/gems.js:241、src/renderer/canvas2.js:743 仍用 `window.prompt()`,在 Electron 渲染进程同样静默返回 null,相关入口点击无反应;待后续按需修复。

## v0.15.12(2026-09-16,已发布)
- 发布:GitHub Release [v0.15.12](https://github.com/891458249/Drafter/releases/tag/v0.15.12)(Release ID 389682348)已发布并设为 Latest;exe(182,816,459 字节)/blockmap(189,248 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.11 → latest 0.15.12, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.12.js` 已入库;首次跑在发布后公开下载校验阶段 ECONNRESET(资产已全部上传),`--verify` 重跑完成全部校验。产物 `dist/release-0.15.12/`。commit `f9f8e799`,tag `v0.15.12`。
- 修复:配置了 gpt-6-astra 等有视频能力的模型,视频附件仍分析失败(HTTP 400 `Invalid value: 'video_url'`)。
  - 根因(实测确认):Kuro 等 OpenAI Responses 风格网关的上游按 Responses API schema 校验入参,**没有任何视频字节入参**——`video_url` 判为非法值、`input_file` 仅支持文档类 MIME 拒绝 video/mp4;v0.15.11 的 video_url 直发在这类网关必然 400。
  - 修复(生产落地):改为**本地抽帧走图像通道**——新模块 src/main/video-frames.js 用 Electron 自带 Chromium 的 `<video>`+`<canvas>` 在隐藏 offscreen BrowserWindow 解码本地视频(≤64MB,零新增原生依赖),按时间轴均匀抽 6 帧(避开首尾黑帧,采样点 (i+0.5)/n)、缩放至 ≤768px 宽、JPEG q0.72 导出 base64;aux-models.js 视频分支把这些帧作为多个 `image_url` 块 + 「按时间顺序的关键帧」中文 prompt 发 chat/completions(OpenAI 式视频理解标准做法)。回退链:抽帧→image_url;失败回退 video_url(兼容 Qwen-VL/GLM-4V 原生视频网关);再失败元信息兜底。
  - 媒体源用 Blob 注入(主进程读字节→base64→页面 atob→Blob→createObjectURL),避免 sandbox 窗口 file:// 源被 opaque origin 拦截;窗口 `sandbox:false, webSecurity:false`(仅加载 about:blank,无外部内容),帧解码错误细分 HEVC/AV1 等原因。
  - 验证:`npm test` **368/368**(aux-models 视频 3 例:多 image_url 块注入抽帧器、400 回退 video_url、无抽帧器直发 video_url);端到端冒烟 `.claude-ui/probe-extract-e2e.js`(真 Kuro key + 真 10.9MB mp4):抽帧 4 帧/79.5s/1280x720 用 864ms,analyzeMedia 经图像通道由 gpt-6-astra 返回完整中文视频内容描述(32.3s)。
  - 坑(Electron 探针):①`ELECTRON_RUN_AS_NODE=1` 残留时 electron.exe 当纯 node 跑且 `process.versions.electron` 仍有值,判定须用 `typeof require('electron')==='string'`;②探针须 `app.on('window-all-closed',()=>{})`,否则抽帧隐藏窗口销毁后默认 quit 会在 analyzeMedia 的 fetch 途中无声杀进程(EXIT 0);③抽帧窗口销毁触发的默认退出是探针特有问题,正式 App 有主窗口不受影响。
  - 文件:src/main/video-frames.js(新)、src/main/aux-models.js(视频分支改图像通道+回退链+getVideoFrameExtractor 惰性加载)、test/aux-models.test.js、.claude-ui/probe-extract-e2e.js(新)。

## v0.15.11(2026-09-16,已发布)
- 发布:GitHub Release [v0.15.11](https://github.com/891458249/Drafter/releases/tag/v0.15.11)(Release ID 389634983)已发布并设为 Latest;exe(182,813,935 字节)/blockmap(189,440 字节)/latest.yml(349 字节)三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.10 → latest 0.15.11, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.11.js` 已入库;首次上传 23% 处 ECONNRESET,重跑经断点续传(自动清不完整 draft 资产)完成。产物 `dist/release-0.15.11/`。commit `085a8bc5`(修复)+`d122df56`(发布脚本),tag `v0.15.11`。
- 修复:视频附件加入后无法被 AI 识别分析——发送时只注入文件名/大小元信息,AI 实际「看不到」视频内容。
  - 根因:src/main/aux-models.js `analyzeMedia` 对 `mediaKind==='video'` 硬编码拒绝(「暂不支持内容分析」),无论是否配置了「视频辅助」模型都直接走元信息兜底;Qwen-VL/GLM-4V/Gemini-OpenAI-兼容等多模态网关实际支持 chat/completions 的 `video_url` 块。
  - 修复:视频辅助改走 `video_url` 块(`data:video/<mime>;base64,…`,≤20MB,mp4/mov/webm 映射 video/mp4、video/quicktime、video/webm),配中文视频分析 prompt(画面/人物/动作/场景/语音转写);网关不支持该块类型时按 HTTP 错误自动落回元信息兜底。3D 模型仍仅注入元信息(chat 接口无二进制入参)。
  - UI:API Keys 弹窗「辅助模型」区加一行说明(图像/音频/视频走所选多模态模型分析,视频需模型支持 video_url 且 ≤20MB;3D 仅注入文件信息)。
  - 文件:src/main/aux-models.js、test/aux-models.test.js(video 2 例+injectMedia 视频 1 例,原「video/model 都拒绝」改为仅 model 拒绝)、src/index.html(辅助模型说明)。
  - 验证:`npm test` **367/367**。注意:需用户在 API Keys 设置里配置「视频辅助」为一个支持视频的多模态模型才会真正分析视频内容;未配置或网关不支持时仍是元信息兜底。

## v0.15.10(2026-09-15,已发布)
- 发布:GitHub Release [v0.15.10](https://github.com/891458249/Drafter/releases/tag/v0.15.10)(Release ID 388933576)已发布并设为 Latest;exe(182,813,736 字节)/blockmap/latest.yml 三件已上传,GitHub SHA-256 与本地一致,公开下载 SHA-256 校验通过;`checkRepoVersion()` 实测 `current 0.15.9 → latest 0.15.10, hasUpdate:true`。发布脚本 `.claude-ui/release-0.15.10.js` 已入库(版本号正则修正为 `0\.15\.10`)。产物 `dist/release-0.15.10/`。commit `be74048f`,tag `v0.15.10`。
- 调整:拆分子任务从 composer 工具栏一次性按钮,改为发送按钮旁的**激活/关闭开关**。
  - 位置:`#btn-split-subtasks` 从 `.composer-toolbar` 移到 `.input-wrap` 内发送按钮左侧,圆形图标按钮(与发送/停止同排)。
  - 行为:点击激活(高亮 `.split-on`)后,用户发出的下一条消息**不直接发送**,而是由 `sendMessage` 拦截交给 `split.maybeSplitOnSend(text)` → 当前会话的 AI 把这条需求拆成多个子任务 → 弹出可编辑确认卡片 → 确认后为每个子任务新建并行 code 会话并行执行。再次点击开关即关闭,恢复正常发送。
  - 拆分失败时仍弹出卡片并显示错误,用户可手动编辑/添加条目后确认,或取消。
  - 文件:src/index.html(按钮移位+改图标)、src/renderer/split.js(改开关态 `armed` + `maybeSplitOnSend` 导出 + 失败兜底弹卡)、src/renderer/input.js(sendMessage 拦截)、src/styles.css(`.split-toggle`/`.split-on` 样式、输入框右 padding 48→80px)、.claude-ui/probe-split-subtasks.js(冒烟改走「激活开关→点发送」路径)。
  - 验证:`npm test` **364/364**;端到端冒烟实测激活开关→发送→真实 HTTP 拆分→可编辑卡片→确认→真实建出 2 个并行 code 会话(标题前缀 `⧉`,继承 cwd/model)。

## v0.15.9(2026-09-15，已发布)
- 新增:拆分子任务。composer 工具栏「⧉ 拆任务」→ 当前会话的 AI 把需求拆成多个子任务 → 弹出可编辑的确认卡片(可改标题/说明、增删条目)→ 确认后为每个子任务新建一个并行 code 会话并行执行。
  - 触发:点「⧉ 拆任务」。需求取自输入框文本;为空时回退取当前会话最近一条用户消息。
  - 拆分:新 IPC `sess:splitSubtasks`(main.js)调 `llmtext.complete`,沿用当前会话的 Key/模型(缺省回退活跃 Key 的首个对话模型),不污染当前会话历史。新模块 `src/main/split-subtasks.js` 宽松解析模型返回(剥 markdown 围栏/首尾散文、兼容 title/name/task 等别名字段、标题截断 60 字、数量上限 12)。
  - 确认卡片:`#split-modal` 可编辑列表(split.js),标题 input + 说明 textarea + 每行删除 + 「＋ 加一条」。
  - 并行执行:`sess:spawnSubtasks` 为每个子任务 `sessions.create` 一个继承当前会话 cwd/项目/模型/Key/权限/推理深度/子 Agent/Gem 的 code 会话,标题前缀 `⧉`,同步入队各自子任务并行跑(不切换用户当前视图)。
  - 验证:解析器单测 8 例;`npm test` **364/364**;端到端冒烟 `.claude-ui/probe-split-subtasks.js`——本地假 OpenAI 服务 + 真 Key,真实 `sess:splitSubtasks` 走通 HTTP 拆分、卡片弹窗可编辑/增删、确认后真实建出 2 个并行 code 会话(继承 cwd/model)。
  - 文件:src/main/split-subtasks.js、src/renderer/split.js、main.js(两个 IPC)、preload.js、src/index.html(按钮+弹窗)、src/styles.css(确认列表样式)、src/renderer/app.js(init)、test/split-subtasks.test.js。

## v0.15.8(2026-09-15，已发布)
- 修复:按住鼠标选择文字时,一选就把上方所有文字(含顶栏/侧栏 UI 标签)一并选中。
  - 根因:全局未设 `user-select`。从消息气泡向上拖拽越过 `#messages` 滚动容器上边界时,Chromium 原生选择把焦点跳到顶栏等可选 UI 元素,导致从顶栏到按下点的所有文字被连带选中。CDP 探针复现:在第 3 条 assistant 消息按下拖到顶栏,选中串包含 `v0.15.7 已就绪/视图/面板/项目/会话` 等 UI 文本,焦点落在 `HEADER.topbar` 的按钮上。
  - 修复:src/styles.css 新增选择约束——UI 骨架(`.topbar/.sidebar/.right-panel/.composer/.msg-role/.msg-nav/.scroll-bottom/.update-chip/.usage-chip/.section-switch/.ctx-menu/.modal-mask/.code-card-head/.thinking/.tool-head/.perm-card/.aigc-card-head/.turn-status/.turn-progress/.composer-foot`)设 `user-select:none`;消息正文 `.bubble`、代码块 `.code-card pre`、输入框/编辑器等文字交互区显式 `user-select:text`。消息气泡外的 UI 不再进入选区,向上越界拖拽只保留消息内容。
  - 验证:`npm test` 356/356;CDP 探针 `.claude-ui/probe-text-selection.js` 对打包版实测——修复前焦点逃逸到顶栏按钮、修复后选区锚点/焦点均停留在按下的消息内。

## v0.15.7(2026-09-14，新增改动未发布)
- 新增：上下文自动压缩，满窗口后不再禁止继续对话——SDK Code/Agent 与极速 Chat 会话在约 80% 自动压缩并在同一会话继续，提供方拒绝超长上下文时自动压缩后恢复。
  - 根因：应用此前未启用 SDK 的自动压缩能力；`system/status` 的 compacting/compact_result 事件被当 `ui_other` 丢弃，压缩边界后旧 usage 仍显示 100%；极速 Chat 的「压缩」按钮被禁用且发送失败后可能永久锁发送。
  - 实现：所有 SDK 会话合并 `settings.autoCompactEnabled:true`+按模型表的保守 `autoCompactWindow`（上限 200k，不抬高提供方硬上限），`CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=80` 提前压缩；换模型经 `applyFlagSettings` 热更新；压缩状态/失败转 `ui_compacting` UI 状态，边界事件使旧用量失效显示「待统计」；result.errors 透传；手动压缩按钮回合中禁用、发送失败可恢复；极速 Chat 重新启用「压缩」按钮。Harness 侧 provider 同步带已知模型 contextWindow（compaction-basic 默认 auto:true、80% 触发、溢出恢复一次）。
  - 文件：src/main/context-compaction.js、src/main/{sessions.js,harness/keys-bridge.js}、src/renderer/{chat.js,app.js,input.js}、test/{context-compaction.test.js,agent-route-live.test.js}。
  - 验证：`npm test` **356/356**；真实 SDK+本地假网关实测：code/chat 在高用量回合自动压缩、压缩边界真实到达、摘要请求真实发出、同一 sdkSessionId 继续对话；提供方 400「prompt too long」自动压缩恢复；手动 /compact 成功；401 不触发压缩重试、中断后原会话仍可继续。隔离 Electron 冒烟（开发版）通过且调试资源清理 ok。
  - 边界：单条消息超过整个窗口或「可压缩历史不足」(too_few_groups) 时无法压缩，UI 明确报错并保留会话可重试；压缩不突破提供方硬上限。本机全局 CLI 会话未改（其自带自动压缩）。
- 新增：全局调试占用释放，覆盖 Drafter 工具会话及本机用户级助手配置。调试收尾须释放本次资源并验证，避免后台进程/调试连接阻止软件更新。
  - 实现：Windows 私有 Job Object 受管启动（挂起创建、入 Job 再运行、关闭 Job 回收后代）；监督器校验 owner PID+创建时间，正常退出优先、有界强制收尾；按 scope/资源隔离状态，连接 release/verify 回调只在内存登记，不能从状态文件执行任意命令。不按软件名杀进程、不退出用户原实例。
  - 接入：SDK PreToolUse/Stop/SubagentStop/失败提示、interrupt/stop/query finally；Harness 会话提示及停止/取消/错误扩展点；应用退出等待清理、更新安装前拒绝带未解除的已登记占用继续。保留现有只读/模型路由守卫和不加载 user settings 的 Key 隔离策略。
  - 本机：追加 `~/.claude/CLAUDE.md`，安装独立 `~/.claude/debug-resources/`，合并 SessionStart/PreToolUse/PostToolUseFailure/Stop/SessionEnd command hooks，原 settings 按时间戳备份；不修改模型、Key 或网关。
  - 文件：src/main/debug-resources/*、src/main/harness/debug-resources-bridge.js、src/main/{sessions,updater}.js、src/main/harness/harness-bridge.js、main.js、scripts/install-debug-cleanup.js、test/{debug-resources,agent-route-live}.test.js、test/packaged-smoke.js、docs/debug-resource-cleanup.md。
  - 验证：`npm test` **348/348**，`git diff --check` 通过；Windows 实测父子进程、owner 退出兜底、TCP 端口重新绑定、独占文件重命名失败→清理后成功；真实 SDK+本地假网关验证 Stop 和 interrupt 清理。开发版 Electron 隔离 userData 冒烟：主界面/存储 IPC/Markdown/终端/Harness 页面正常，最终报告已登记资源清理 `ok:true`；截图超时失败路径同样清理通过，改主窗口截图后冒烟成功。
  - 尚未验证：独立用户级新会话的 hooks 自动触发（脚本 stdin 与 JSON 结构已验证，当前 Drafter 会话不读 user 设置，需 `/hooks` 或重启）；Harness 真实模型回合中的取消/停止清理，仅验证实际 boot/页面；未构建新安装包、未发布、未安装更新。
  - 边界：自动验证只覆盖受管/登记资源，文件锁须针对软件额外检查；用户仅中断一轮但 owner 仍存活时，本机 Stop 不保证触发，必须由 finally/显式 cleanup/SessionEnd 收尾；不承诺任意第三方软件自动解锁，不支持受管 Job 脱离保留。

## v0.15.7(2026-09-10，已发布)
- GitHub Release：[Drafter v0.15.7](https://github.com/891458249/Drafter/releases/tag/v0.15.7)（Release ID 386101043），安装包、blockmap、latest.yml 已上传并设为 Latest；三个附件的 GitHub SHA-256 与本地一致。发布脚本 `.claude-ui/release-0.15.7.js` 先上传草稿再发布，可安全重试不完整上传；`RELEASE.md` 明确了自动更新必须发布附件，不能只推送代码和标签。
  - 发布验证：公开 `releases/latest/download/latest.yml` 与本地完全一致；公开下载完整安装包和 blockmap 后 SHA-256 校验通过；调用应用实际 `checkRepoVersion()`，返回 `current: 0.15.6, latest: 0.15.7, hasUpdate: true`。未操作用户正在运行的应用，也未自动安装更新。
- 项目检查修复：
  - 只读保护：统一检查 Agent 的 PreToolUse、权限回调与挂起权限卡；阻止 shell/MCP/子 Agent 等无法确定写入范围的调用，保留读取和对其他文件的受控编辑。规范化相对路径、Windows 大小写、目录链接、硬链接及 NTFS 文件流，检查异常时拒绝执行。此保护覆盖 Drafter Agent 调用，不替代操作系统权限或限制外部进程。
  - 配置恢复：配置损坏不再静默退回空数据；有效 `.bak` 可恢复；无可用备份时保留原件、停止保存并显示启动错误。保存采用临时文件、fsync、原子替换和上一版备份，恢复写入前另存损坏原件，读写错误向调用者传递。
  - 网关设置：`agentSettingSources()` 按规范化路径身份比较，覆盖 Windows 大小写及目录链接，补齐本版本原有修复。
  - Markdown：原始 HTML 按文本显示，屏蔽危险链接协议与实体混淆，保留普通 Markdown、表格、代码块和本地文件链接。
  - 构建：移除 tsdown 中的本机绝对路径；新增 `npm run build`/`build:check`，`npm run dist` 自动重建 Harness Web/IPC 产物且明确禁止自动发布。保留仓库中已打补丁的 Harness lib/vendor-deps。
  - 验证工具：增加原子存储、只读边界、Markdown 和真实 SDK 权限回归；提供包内源码比对、安装包哈希校验及独立用户目录的打包态冒烟脚本。
  - 文件：main.js、package.json/package-lock.json、tsdown.config.ts、README.md、RELEASE.md、src/main/{path-identity,projects,sessions,store}.js、src/renderer/{markdown,state}.js、src/index.html、build/{prepare-harness,verify-package}.js、test/{store-recovery,readonly-boundary,markdown,chat-fast,migrations,agent-route-live}.test.js、test/packaged-smoke.js、docs/project-audit-2026-09-10.md、本文件。
  - 验证：`npm test` **337/337**；Harness 构建通过；NSIS 安装包构建通过；87 个应用文件与包内内容一致；claude.exe 解包正确；安装包 SHA-512 与 latest.yml 一致。真实打包态主界面、设置 IPC、Markdown 防注入、node-pty 终端、Harness 引擎与前端均通过冒烟。
  - 产物：`dist/release-0.15.7/Drafter Setup 0.15.7.exe`（182,801,108 字节）、对应 `.blockmap` 与 `latest.yml`。产物使用独立目录，旧版本产物保留。
- 修复:chat 会话切到 Agent 模式必报 `Failed to authenticate. API Error: 403 模型未配置:k3(原始错误:forbidden: model not defined: k3)`,极速模式正常。
  - 根因:chat 会话默认 cwd = 用户主目录;Agent 模式 `settingSources: ['project','local']` 使 claude.exe 把 `<cwd>/.claude/settings.json` 当项目设置加载,而 cwd 为主目录时该文件就是用户级 `~/.claude/settings.json`——其 env 把 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` 钉到 CLI 配置的 Kuro 网关,且 settings env 优先级高于 App 按会话 Key 注入的进程凭据(v0.11.6 排除 `user` 源的同一个坑,换 `project` 入口复活);`--model k3` 仍生效 → 带 Kimi 模型名打到 Kuro → 403。极速模式 `settingSources: []` 不加载文件设置故正常。实测 claude.exe 对 settings 不做向上回溯,仅 cwd == 主目录精确命中。
  - 修复:`sessions.js` 新增 `agentSettingSources(cwd)`——`cwd/.claude` 与 `CLAUDE_CONFIG_DIR` 重合时返回 `[]`(视为无项目设置),否则照旧 `['project','local']`。
  - 文件:src/main/sessions.js、test/chat-fast.test.js(+3 例)
  - 验证:npm test 321/321;用打包版 claude.exe + Kimi 凭据在主目录实测复现原错误、空 setting-sources 恢复正常。
- 工程规范:新增本文件;全局规则「所有改动必须写入版本更新.md」已写入 ~/.claude/CLAUDE.md(供其他 AI 检查者核对)。

## v0.15.6 及更早
- 详见 git 提交历史与 GitHub Releases(本文件自 v0.15.7 起建立)。
