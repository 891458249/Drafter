# Drafter · 项目组共享记忆

(所有会话共用。跨会话需要记住的结论、决定、进展请追加到这里。)

- 2026-08-06 诊断:「设为项目文件夹」(proj:adoptDir, main.js:227) 只改 projectId 不切 cwd,导致会话主工作目录停在 C:\Users\dingyongzhen;叠加 ~/.claude/settings.local.json 残留的 permissions.additionalDirectories:["D:\ClaudeUI"],使无关路径出现在所有会话。修复方案:adoptDir 同步切换 cwd(含重启 query)、修存量 store 数据、删 settings.local.json 残留项。
- 2026-08-06 已修复(代码在仓库):①main.js proj:adoptDir 切换 cwd+清冗余 extraDirs+重启 query;②sessions.js addDir/start() 过滤与 cwd 相同的目录;③input.js adopt 分支同步 meta.cwd/state.cwd;④desktopui+claude-ui 两个 store 的 3 个存量会话 cwd 已修正(原文件留了 .bak-cwdfix 备份);⑤~/.claude/settings.local.json 的 additionalDirectories 已删。npm test 75/75 过。注意:正在运行的 App 需重启进程才生效。
- 2026-08-06 v0.9.7 已发布:commit c324e46(0.8.1→0.9.7 全部改动)已 push 到 891458249/Drafter;GitHub Release v0.9.7 已创建并上传 exe(147MB)+blockmap+latest.yml(electron-updater 可自动更新)。发布脚本:.claude-ui/release-0.9.7.js(复用 git credential 取 token,未入库)。注意:正在运行的 App 需重启进程才生效。
- 2026-08-06 坑:改存量会话的 cwd 会导致 resume 失败——Claude Code 按 cwd 分会话记录目录(~/.claude/projects/<cwd编码>/<sessionId>.jsonl),cwd 一变就在新目录找不到旧记录,报 "No conversation found with session ID" 会话终止(App 会自动开新会话接续)。cwd 修复的 3 个存量会话因此各中断过一次,属一次性代价。若将来要改 cwd 又保上下文:把旧目录下的 <sessionId>.jsonl 复制到新 cwd 对应目录再 resume。
- 2026-08-06 v0.9.8 完成并已打包:①会话项「重命名/Side chat/归档/删除」改为右键菜单(state.js 新增通用 showCtxMenu,project 菜单同步复用);②新模块 msgmenu.js:文本消息右键复制(选区优先)/引用(markdown 引用块进输入框),图片(.msg-img 与 .aigc-media)双击进查看模式(#img-viewer 复用 modal-mask,滚轮缩放)、右键查看/复制(fetch→位图→PNG 写剪贴板,兼容 data: 与 aigc://);③注意坑:contextmenu 关闭旧菜单的全局监听要跳过 e.defaultPrevented,否则右键之间互开新菜单会失效。npm test 75/75;dist\DeskTopUI Setup 0.9.8.exe 已构建(exit 0)。运行中的 App 需重启生效。
- 2026-08-06 v0.9.9 完成:①用户消息锚点 uuid——send 打戳进 SDK 流式消息+echo 持久化,user/assistant 事件捕获 uuid,eventKey 去重时忽略 uuid;②msgnav.js 右侧用户消息导航条(点击定位/滚动联动,rAF 合帧);③用户消息右键「修改并重新生成」(locateEcho 校验→writeSessionEvents 截断→fork 到 echo 前 assistant 锚点 resumeSessionAt 重发;无锚点报错不丢上下文)与「从此消息分支」(branchSlice 复制回合前缀+fork 到回合末锚点);store 新增 writeSessionEvents/locateEcho/branchSlice,test/session-edit.test.js 6 例。注意坑:editRegenerate stop() 后要 setImmediate 等旧 _pump finally,否则覆盖新回合 busy。npm test 81/81。
- 2026-08-06 v0.9.9 安装包已构建:dist\DeskTopUI Setup 0.9.9.exe(exit 0),含消息导航/编辑重生成/分支。尚未 commit/publish。
- 2026-08-07 v0.9.10(未 commit):修复「新会话设为新建文件夹为项目路径后会话作废」。根因:adoptDir 切 cwd 后 resume 在新 cwd 的 projects 目录找不到 <sdkSessionId>.jsonl → "No conversation found" → 进程退出;且 sdkSessionId 仍存,之后每次 send 都 resume 失败,会话永久卡死。修复:sessions.js 新增 encodeCwdForProjects(与 claude.exe 内 A0 完全一致:[^a-zA-Z0-9]→'-',超 200 字符截断+att 哈希 base36 后缀,已从二进制提取验证)+migrateTranscript(复制记录到新 cwd 目录,登记的 prevCwd 找不到时兜底全盘扫描,覆盖连续两次 adopt);adoptDir 登记 meta.prevCwd;start() resume 前迁移,迁移后仍无记录则清 sdkSessionId 降级全新会话(发 ui_error 提示),防止卡死。注意 CLAUDE_PROJECTS_DIR 遵循 CLAUDE_CONFIG_DIR。test/transcript-migrate.test.js 6 例,npm test 87/87。
- 2026-08-07 v0.9.10 同批:修复「一个会话流式执行时其他会话输入框卡死无法打字」。根因:chat.js appendText 对每个 text delta 把已累积全文 renderMarkdown 重渲一遍(O(n²)),所有会话共用渲染主线程,后台会话流式输出占满主线程。修复:流式渲染 80ms 合帧节流(scheduleAssistantRender),finalizeAssistant/content_block_start/handleAssistantMessage 文本块切换时 flushAssistantRender 冲销保证完整。chat.js 无单测覆盖,npm test 87/87 无回归。
- 2026-08-07 v0.9.10 已发布:commit bc02b18(v0.9.8-v0.9.10 全部改动)已 push 到 891458249/Drafter;GitHub Release v0.9.10(id 366501018)已创建并上传 exe(147MB)+blockmap+latest.yml(electron-updater 可自动更新)。发布脚本:.claude-ui/release-0.9.10.js。注意:正在运行的 App 需重启进程才生效。
- 2026-08-07 v0.9.11 Gem 自定义助手完成(未 commit/package):store settings.gems{name,desc,instructions≤30000,tools,model,knowledge≤10,knowledgeEnabled,preset};主进程 src/main/gems.js(CRUD/composeAppend/composeMediaPrefix/4 预置播种)+main.js gems IPC(gems:rewrite=✨AI 优化,按角色/任务/情境/形式四要素)+sess:setGem+aigc:send 前缀;sessions.js create/start 注入(gem append 与 projCtx append 合并)/setGem 走 needRestart 重启 resume;渲染端 gems.js 三栏管理 modal(对齐 Gemini 编辑器:列表/表单含默认工具多选+知识文件/预览含近期对话+开始对话)+composer 💎 选择器+会话徽标。冒烟注意:须 unset ELECTRON_RUN_AS_NODE 再启动 electron,否则 electron-updater 崩(预存环境坑,与本功能无关)。npm test 93/93(gems.test.js 6 例)。
- 2026-08-07 v0.9.12 聊天代码块 IDE 化完成(未 commit):新模块 codeblock.js——enhanceCodeHtml 把 marked 的 <pre><code> 包成代码卡片(头部条:语言标签+复制按钮,复制原文存 data-code,clipboard 直写,#messages 委托);hljs.js 新增 highlightAs 按 fence 语言着色(别名 py→python/sh→bash/ts→typescript 等,未注册退化 highlightAuto,主题沿用 github-dark.css);state.js 新增 PRE_CODE_RE/decodeCodeHtml。覆盖流式/用户气泡/计划卡片所有 renderMarkdown 注入点;msgmenu 复制整条剔除 .code-card-head。test/codeblock.test.js 4 例(window.hljs/document stub),npm test 97/97。注意坑:测渲染 ESM 模块须 stub global.document(state.js 顶层挂监听)。
- 2026-08-07 v0.9.11+v0.9.12 已发布:commit 071305f 已 push;GitHub Release v0.9.12(id 366671375)已创建并上传 exe(147MB)+blockmap+latest.yml。发布脚本 .claude-ui/release-0.9.12.js(未入库,同前两个 release 脚本)。
- 2026-08-07 v0.9.13(未 commit):修复「流式输出时上翻被强制拉底」。chat.js 引入 stickToBottom 吸附态(scroll 监听,距底 <80px 吸附,上翻解除),scrollBottom 加 force 参数;force 仅用于用户发消息/切换会话/历史回放;非吸附态显示「↓ 回到底部」悬浮按钮(index.html btn-scroll-bottom,.chat-col 已 relative)。npm test 97/97。
- 2026-08-07 v0.9.13 同批(未 commit):修复「部分网关(Kimi)流式 message_delta 不带 usage → 回合状态恒 0 tokens」。chat.js handleAssistantMessage 用 assistant 完整消息的 message.usage 兜底补计 turnTokens(message_stop 记 msgDeltaCounted 防重复计),并刷新 lastUsage + 发 usage-updated 事件;app.js 监听实时刷新「上下文 %」按钮(此前只在 turn-done 更新)。npm test 97/97。
- 2026-08-07 v0.9.13 同批(未 commit):turn-status 行下加回合进度条(.turn-progress 往复滑动指示条,agentic 回合无真实百分比)+ 当前动作提示(s.ui.curAction:思考中/撰写回复/调用工具 X/子任务 X,流式事件跟踪,合入状态行文本尾部)。npm test 97/97。
- 2026-08-07 v0.9.13 同批 3(未 commit):进度条升级为预测式——时间双曲线 predictedPct=92·t/(t+25000) 渐近 92%(25s~46%,100s~74%),bar 改 width+transition 驱动;result 时补满 100% 停 350ms 再隐藏(busy 重入不隐藏);状态行尾部加 ~N% 量化。npm test 97/97。
- 2026-08-07 v0.9.13 同批 4(未 commit):回合结束每次都发 Windows 右下角 toast(sessions.js onTurnDone 去掉 activeId 限制,标题区分出错,正文带用时);非活跃会话仍额外打点。npm test 97/97。
- 2026-08-07 v0.9.13 同批 5(未 commit):系统托盘驻留——close 拦截 hide(app.isQuitting/darwin 除外),托盘左键唤出/右键菜单「显示/退出」;坑:updater.installAndRestart 必须先置 app.isQuitting 否则关窗拦截挡住自动更新;second-instance 改 showMainWindow;build/icon.png 加入打包 files;cleanup() 幂等复用。npm test 97/97。
- 2026-08-10 v0.9.13 已发布:commit 7afdbeb 已 push;GitHub Release v0.9.13(id 367658671)已上传 exe+blockmap+latest.yml。发布脚本 .claude-ui/release-0.9.13.js(未入库)。顺带把 docs/mobile-remote-plan.md 一并入库了。
- 2026-08-10 v0.9.14(未 commit):消息导航条 Dock 式悬停放大(msgnav.js mousemove 按光标距离二次衰减 scale,最大 1.35x,transform-origin right;仅内容不溢出时启用 .mag,可滚动时退回普通 hover——overflow-y:auto 时 transform 放大必被裁切)。npm test 97/97。
- 2026-08-10 v0.9.15 已发布:commit 8040477 已 push;GitHub Release v0.9.15(id 367665021)已上传 exe+blockmap+latest.yml。内容:v0.9.14 Dock 放大+瞬时跳转 + v0.9.15 ⋯ 菜单「⚡ 瞬时跳转定位」设置项(state.instantJump,默认开,作用于导航定位与回到底部;关闭恢复平滑滚动)。发布脚本 .claude-ui/release-0.9.15.js(未入库)。注意:本轮改动部分由并行会话同步编辑完成(chat.js/app.js 的 instantJump 接线),合并后 npm test 97/97。
- 2026-08-10 v0.9.17 已发布:commit 9068a38 已 push;GitHub Release v0.9.17(id 367676980)已上传 exe+blockmap+latest.yml。内容:①sticky 复制按钮(前一条目);②**数据迁移/自愈框架 src/main/migrations.js**(解决「每次更新后旧会话出问题」):repairs 每次启动幂等跑——transcript 健康扫描(不在当前 cwd 目录则 migrateTranscript 迁移,全盘皆无则清 sdkSessionId 降级+记 resumeLostAt)、prevCwd 残留清理、会话 meta 按 id 去重;migrations 按版本一次性跑,游标 settings.dataVersion 只前进不后退,单个失败不影响其他;报告写 userData/logs/migrations.log。铁律:只修不删,修不好就降级。今后破坏性数据格式变更必须在 MIGRATIONS 登记(规则已写进 RELEASE.md)。test/migrations.test.js 7 例,npm test 104/104。坑:PowerShell Set-Content 默认 GBK 会毁 UTF-8 中文,生成/改写含中文的脚本文件要用 Write 工具。
- 2026-08-10 v0.9.18 已发布:commit 34a8a42 已 push;GitHub Release v0.9.18(id 367684372)已上传 exe+blockmap+latest.yml。修「消息导航悬停上下缩放不平均」:v0.9.16 行内改 width/height 会推挤下方项+距离按变动中的 rect 测量产生反馈抖动;改为**槽位+transform** 结构——.msg-nav-slot 槽位布局恒定 26×4,按钮全尺寸 190×22 绝对定位槽内,默认 scale(0.137,0.182) 成横杠,悬停只改 transform/opacity/zIndex,距离按槽位中心测量(槽位不动),上下严格对称。可滚动回退 scale(0.674,1)。教训:Dock 式放大一律用 transform(布局不变),不要改布局尺寸。npm test 104/104。
- 2026-08-10 移动端远程控制策划案已产出:docs/mobile-remote-plan.md。核心思路:手机端=第二个渲染端,新增 remote-server.js 做现有 IPC 的 WebSocket 翻译;一期局域网直连+PWA(扫码配对/TLS/远程强制审批),二期云端中继或 Tailscale 解决异地;1 人 3-4 周到 MVP,建议作为 v1.0.0 旗舰特性。
- 2026-08-10 v0.9.16 已发布:commit 0aed100 已 push;GitHub Release v0.9.16(id 367671955)已上传 exe+blockmap+latest.yml。内容:①#msg-nav 收进 .messages-wrap(只包裹 #messages,flex:1),不再覆盖输入发送栏;②导航项默认改小横杠(26×4、字号 0,类 GPT),悬停按光标距离二次衰减展开 width/height/font-size(替换 v0.9.14 的 scale 方案,行内尺寸+CSS transition),溢出可滚动时退回 CSS :hover 整项展开。注:v0.9.16 的 HTML/CSS 部分由并行会话先行改好,本会话补 msgnav.js 并发布;release-*.js 脚本本轮起已随 commit 入库(不含 secret,走 git credential)。npm test 97/97。
- 2026-08-10 v0.9.17 sticky 部分(已发布):代码卡片头部条 sticky——.code-card-head position:sticky;top:0,长代码块滚动时复制按钮吸附可视区顶部。关键坑(此前并行会话 Edit 失败未落地的那个改动):.code-card 的 overflow:hidden 会破坏 sticky,必须去掉,圆角改由头部条(7px 7px 0 0)承担;头部背景改实色 #1e1c19 防吸顶时代码穿透。纯 CSS,sticky 链路无其他 overflow。
- 2026-08-10 v0.9.19 已发布:commit 0f167e4 已 push;GitHub Release v0.9.19(id 367688150)已上传 exe+blockmap+latest.yml。消息导航三项调优:①.msg-nav align-items:center 垂直居中;②装不下时(非 mag)列表上下 28px mask-image 线性淡出=两侧省略效果;③悬停对比强化——衰减 k 再取 1.5 次方+半径 90→70,中心项最大 1.15x 并打 .hot 高亮(主题色描边/底色/文字实色),远处项压暗 0.35;可滚动回退 hover 同步高亮配色。npm test 104/104。
- 2026-08-10 v0.9.20 已发布:commit a114a6f 已 push;GitHub Release v0.9.20(id 367689206)已上传 exe+blockmap+latest.yml。导航间距防误触:gap 6→10px、横杠 4→6px 厚(pitch 10→16px);缩放系数同步 scale(0.137,0.273)/SY0=6/22。npm test 104/104。
- 2026-08-10 v0.9.21 已发布:commit 8275410 已 push;GitHub Release v0.9.21(id 367691144)已上传 exe+blockmap+latest.yml。导航溢出态重做:列表 max-height 100%→34%(约 1/3 会话高)+居中;非 mag 时 mousemove 按光标在小窗内纵向比例代理 scrollTop(=ratio·(scrollHeight-clientHeight),光标所指即整体同比例处),scrollbar-width:none 隐藏滚动条,上下淡出保留;mag 态 Dock 放大不变。npm test 104/104。
- 2026-08-10 v0.9.22 已发布:commit 9f49735 已 push;GitHub Release v0.9.22(id 367715886)已上传 exe+blockmap+latest.yml。修「悬停范围太小+上下只显示半条」:①mousemove/mouseleave 提升到整条 rail(pointer-events:auto,整条右缘皆悬停区);②非 mag 时小窗跟随光标——windowTop=ratio·(H−W) 与 scrollTop=ratio·(total−W) 同一 ratio 驱动,光标始终正指总列表同比例项且总在窗内可点;③scrollTop 按 pitch 16px 吸附取整,边缘不再半条;④rebuild 清 list transform。代价:rail 132px 整条挡消息区右缘文字选择/点击。npm test 104/104。
- 2026-08-10 v0.9.23 已发布:**v0.9.22 被用户要求回撤**(git revert 9f49735,恢复 v0.9.21 居中小窗+窗内比例代理滚动;v0.9.22 的 GitHub Release 未删,被 v0.9.23 取代为 latest);新功能:边界淡出分侧——非 mag 按 scrollTop 维护 at-top/at-bottom class,未探到边界的一侧保持淡出,探到顶/底即去除该侧(scroll 监听驱动,代理滚动/滚轮都覆盖)。commit 85f0b86 已 push;Release v0.9.23(id 367720726)已上传。npm test 104/104。
- 2026-08-10 v0.9.24 已发布:commit 307dd8b 已 push;Release v0.9.24(id 367733097)已上传。修「更新后依旧没有显示完全」:v0.9.23 回撤时把 v0.9.22 的 scrollTop 整项吸附一并回掉,代理滚动连续值导致窗口上下边缘项只显示半条;补回按项距 16px 取整吸附(PITCH=16,槽位 6+间距 10),边缘项要么完整显示要么完整隐入淡出区,分侧淡出不受影响。npm test 104/104。
- 2026-08-10 v0.9.25 已发布:commit 构建后已 push;Release v0.9.25(id 367738875)已上传。三项:①导航禁滚轮(list wheel preventDefault passive:false);②回到底部按钮从 chat-col 移进 messages-wrap 右下角(right:12 bottom:10,z-index 50 压过 rail 40);③**macOS Dock 式边缘感应**——mousemove/mouseleave 提升到整条 rail(pointer-events:auto),mag 态光标 Y 钳到首/末槽位中心之间(上缘之上首项满放大,下缘同理),非 mag 态 ratio 钳 0/1 探边界。注意:rail 132px 整条再次挡住消息区右缘条带的文字选择(同 v0.9.22 代价,用户要求如此)。npm test 104/104。
- 2026-08-10 v0.9.26 已发布:commit 4c9645b 已 push;Release v0.9.26(id 367743336)已上传。修「Gem 菜单被窗口底边裁切」:gems.js showMenu 固定向下展开 → 先渲染量高,下方空间不足则向上展开(top=锚点.top−6−高,钳≥8)并按可用空间收 maxHeight;共享 showMenu 的默认工具菜单同享。npm test 104/104。
- 2026-08-10 v0.9.27 已发布(两会话协作):本会话 commit 25bd77b=**文本附件改路径引用**(发送侧只登记 {name,path},粘贴无路径落盘 userData/attachments/ 经新 IPC files:savePasted;二进制检测改 files:sample 4KB 采样;渲染侧 chat.js ATTACH_BLOCK_RE 把附件块含旧内联全文折叠成 📄 卡片 .file-attach-chip);并行会话 commit 7704b05=导航密度自适应+边界渐出去遮罩化。**发布踩坑**:并行会话先建 Release 传了只含其特性的构建,且 **GitHub 把上传资产名的空格净化成点**(DeskTopUI.Setup.0.9.27.exe),release-*.js 按空格名查 existing 查不到→重复上传 422;fix-release-0.9.27.js 删旧资产+传合并构建(25bd77b 含两特性)+合并说明,已修复。教训:release 脚本查 existing 要同时匹配 name.replace(/ /g,'.')。npm test 104/104。
- 2026-08-10 v0.9.28 已发布:commit 已 push;Release v0.9.28(id 367904582)已上传。附件重定义:①不限大小(图片去 5MB 上限;文本附件 v0.9.27 起只按路径引用本无上限);②双击附件卡片(消息气泡 .file-attach-chip data-path,msgmenu.js dblclick 委托;输入框待发区 chip 同)→emit('open-file') 打开编辑器面板查看;③files.readFile 上限 2MB→20MB 让大附件能开。release-0.9.28.js 起查 existing 已兼容点命名。npm test 104/104。
- 2026-08-10 v0.9.29 已发布(此行曾因 GBK 写坏含 U+FFFD,2026-08-12 按残留语义重建):commit ba3826a 已 push;Release v0.9.29(id 368413623)已上传。**彻底修「附件截断/乱码」严重 BUG**,三根因:①AI 侧——Read 默认只读前 2000 行,引用块改为显式指令「用 offset/limit 分段读完,禁止只读开头」;②编辑器 files.readFile 20MB 上限彻底移除;③乱码误判——GBK/ANSI 代码文件按 UTF-8 硬解出乱码被拒收,新增 files.decodeBuffer(BOM→严格 UTF-8→GBK 兜底),readFile/sampleFile/粘贴流(读 ArrayBuffer 而非 readAsText)全部接入;savePastedAttachment 补回落盘二进制校验(此前校验随 renderer 分支删除丢失)。test/file-decode.test.js 5 例,npm test 109/109。
- 2026-08-11 v0.9.31 已发布:commit 806ee60 已 push;Release v0.9.31(id 368448451)已上传。①**皮肤系统 themes.js**(4 套:深色默认/浅色/暗夜蓝/墨绿,主题=CSS 变量覆盖集写 documentElement,settings.theme 持久化,boot 最先应用);②**独立设置面板 settings-modal**——原 ⋯ 下拉(more-menu)全部入口并入(外观皮肤卡片即时预览/功能六入口/偏好瞬时跳转 checkbox/其他两入口),btn-more 直达面板,删除 more-menu;③活跃会话项对比修复:.session-item.active 改 accent 洗底+描边,并发现 **--accent-bg/--bg-hover 被引用却从未在 :root 定义**(msg-nav active/.hot 高亮此前一直无底色),已补齐。npm test 109/109。
- 2026-08-10 v0.9.27 已发布:commit 7704b05 已 push;Release v0.9.27(id 367756803)已上传 exe+blockmap+latest.yml;发布脚本 .claude-ui/release-0.9.27.js。消息导航两项重做:①**密度自适应**(修「消息过多时显示不正确」):废除 34% 高小窗+悬停代理滚动为主路径——updateDensity() 按消息数把槽距/横杠厚写进 CSS 变量(--pitch/--bar-h/--bar-k),装不下时等比压缩(最小槽距 3px),所有项始终全部可见、Dock 悬停放大任何数量可用;仅极端数量(压到 3px 仍装不下)退回可滚动小窗。mousemove 的槽距/半径(=max(40,pitch×4.375),16→70 同旧值)/SY0 全部改从 CSS 变量回读;window resize 重算密度。注意:CSS scale() 里不能 calc(长度/数) 得无量纲数,故 --bar-k 由 JS 算好写入。②**边界渐出去遮罩化**(用户要求):删掉 :not(.mag) 的 mask-image 分侧淡出全套(at-top/at-bottom 废除),updateFade 改为按槽位中心距可视窗口上下缘 28px 内线性逐项改按钮自身 opacity(基准 active/hover=1 否则 0.6,用 b._fade 标记避免清掉 Dock 悬停写的 opacity);markActive 切 .active 后顺带 updateFade 重算基准。npm test 104/104。
- 2026-08-10 v0.9.30 已发布:commit 已 push;Release v0.9.30(id 368419488)已上传。**修严重 BUG「停止后再编辑重生成会话卡死」**:根因——editRegenerate 的 `await this.interrupt()` 对已 interrupt 过的同一 query 发起第二次 interrupt,SDK 第二次承诺永不 resolve(没有可打断的回合),sessEditRegenerate 永不返回。修复(sessions.js):①interrupt() 幂等化——_interrupting 复用在途承诺 + Promise.race 6s 超时,落地校验 this.q===发起时 q 才清 busy;②editRegenerate 的 stop→重启等待从 setImmediate 改为轮询(30ms×100 上限 3s,this.q 已换/running 已落)——旧泵 for-await 要等 claude 子进程退出才 finally,一个任务间隙不够;③_pump finally 校验 this.q===本泵 q 才写 running/busy(旧泵不覆盖新 query 状态,旧权限卡照常了结);stop() 解除在途 _interrupting。npm test 109/109。

- 2026-08-12 v0.9.32 已发布:commit 8325de9 已 push;GitHub Release v0.9.32(id 368982853)已上传 exe+blockmap+latest.yml;发布脚本 .claude-ui/release-0.9.32.js(未入库)。修复「会话中代码落地路径不可点击」。根因:chat.js linkifyPaths 正则字符类不含冒号,Windows 盘符路径(C:\…)永远匹配失败,且只认行内 code。修复:新模块 filelink.js(parseFilePath 纯函数:盘符/UNC/POSIX/相对路径+行号后缀+尾标点剥离+URL/版本号误报排除;PATH_IN_TEXT_RE 正文散文绝对路径扫描);chat.js linkifyPaths 重写(code 整段识别+TreeWalker 正文文本节点包 .file-link),renderUserBubble 同步接入;editor.js openFile 兼容 {path,line} 负载并 scrollToLine(预览按行高估算,编辑模式选中行)。右侧编辑器面板/open-file 事件/file-link 样式此前均已存在,本次只补识别。test/filelink.test.js 9 例,npm test 118/118。运行中 App 需重启生效。

- 2026-08-12 v0.9.33 已发布:commit b658346 已 push;Release v0.9.33(id 369172381)已上传 exe+blockmap+latest.yml;发布脚本 .claude-ui/release-0.9.33.js(已入库)。三连修——①链接点击主窗口变浏览器——main.js createWindow 加 will-navigate 拦截 http(s) 外抛 shell.openExternal + setWindowOpenHandler deny(http 外抛,file:// 放行);此前渲染的 markdown <a> 默认就地导航且无法返回。②Chat 板块也开右侧面板(Chat 也处理代码任务)——btn-panel 由 code-only 改新类 workspace-only(styles.css: body:not(.sec-code):not(.sec-chat) 隐藏),app.js setSection 仅媒体板块隐藏 right-panel;变更/编辑器/预览/任务/终端五个 tab 在 chat 直接可用,diff 非仓库有兜底。③窗口过窄 composer 工具栏乱版——.composer-toolbar 加 flex-wrap:wrap + 子项 flex-shrink:0/white-space:nowrap(此前 flex 默认 shrink 把中文按钮挤成竖排)。npm test 118/118。运行中 App 需重启生效。

- 2026-08-12 v0.9.34(未 commit):**ClaudeUI 品牌全面清理**。会污染用户项目的:projects.js 项目组注入改 <desktopui-project-group>+「DeskTopUI 的项目组」,共享记忆目录新建用 .desktopui(存量 .claude-ui 继续识别,memoryDir 双探,memory.md 头部已改 DeskTopUI);gems.js 注入改 <desktopui-gem>;git.js worktree 改 .desktopui-worktrees + desktopui/ 分支前缀(files.js IGNORE 双名)。应用身份:appId com.claudeui.app→com.desktopui.app(注意:旧版自动更新装新版时旧安装目录 Programs\claude-ui 不被接管,会并存,需手动卸载一次;userData 由 productName 派生不受影响);legalTrademarks→DeskTopUI;图标 CU→DU(make-icon.js 已重生成)。顺手修 package.json copyright GBK 坏字(漏→©)与 package-lock 过期 name/version。本地更名:claude-ui-store.json→desktopui-store.json(store.js 读旧兜底、legacy 不删),CLAUDE_UI_USERDATA→DESKTOPUI_USERDATA(旧名兼容),Claude UI.bat→DeskTopUI.bat,README/FAILURES/REGRESSION/RELEASE 名称清理。测试 sweep 'claude-ui-' 前缀 14 文件+新增 store legacy 兜底/projects memoryDir 2 例,npm test 120/120。剩余有意保留:legacy 兼容代码、历史发布脚本、DEVLOG 历史行(171 已加修订注)、D:\ClaudeUI 本地目录名。

- 2026-08-12 v0.9.35 已发布:commit d128532 已 push;Release v0.9.35(id 370487818)已上传 exe+blockmap+latest.yml;发布脚本 .claude-ui/release-0.9.35.js(已入库)。内容:**更名 Drafter**(v0.9.34 DeskTopUI 中间品牌未发版,被本轮取代)。productName/窗口标题/托盘/legalTrademarks/appId(com.drafter.app)/包名/快捷方式全改 Drafter;**userData 自动迁移**——main.js 启动时 %AppData%\Drafter 不存在则从 DeskTopUI/desktopui/claude-ui 整体复制(只复制不删,缓存不搬,DRAFTER_USERDATA 覆盖时不迁);store 兜底链 drafter→desktopui→claude-ui 三个文件名;projects memoryDir 三代识别(.drafter→.desktopui→.claude-ui);worktree .drafter-worktrees+drafter/;Gem 注入 <drafter-gem>;Logo 重设计为层叠草稿纸(make-icon.js 重生成);appId 变更致旧安装目录不被接管需手动卸载一次。测试 sweep 33 处前缀,store/projects 兜底测试更新,npm test 120/120。新发布脚本资产名改 Drafter Setup x.y.z.exe。

- 2026-08-12 v0.9.36 已发布:commit 73d957e 已 push;Release v0.9.36(id 372207239)已上传 exe+blockmap+latest.yml;发布脚本 .claude-ui/release-0.9.36.js(已入库)。内容:系统通知可点击跳转会话。sessions.js notify 加 onClick 参数(Notification 持引用进 Set 防 GC 吞 click 监听,close 释放)+ jumpToSession(restore/show/focus 主窗口 + 发 sess:activate);onTurnDone 与 notifyPermission 都接上点击;preload api.on 白名单加 sess:activate;app.js 监听后 ensureSession(sessList 取 meta 兜底)+setActiveSession+refreshList,跨板块会话由 session-activated 自动切板块。npm test 120/120。运行中 App 需重启生效。

- 2026-08-12 v0.9.37 已发布:commit 25582d5 已 push;Release v0.9.37(id 372284068)已上传 exe+blockmap+latest.yml;发布脚本 .claude-ui/release-0.9.37.js(已入库)。内容:权限确认 toast 不再限非活跃会话——notifyPermission 去掉 activeId 门控,任何板块/活跃会话/窗口最小化都弹右下角通知(权限是阻塞等待需强提醒),点击跳转到该会话(v0.9.36 链路);非活跃仍额外打点。npm test 120/120。

- 2026-08-12 v0.9.37 同批(已发布):**权限模式热切换**。三层保障:①setPermissionMode 先把挂起的权限卡按新模式即时裁决(bypass/acceptEdits→allow 编辑类,dontAsk→deny,卡片同步消失);②SDK 控制请求 set_permission_mode 照发(失败不再静默吞,记日志);③_onPermission 按最新 meta.permissionMode 本地兜底——bypassPermissions 全放行、acceptEdits 放行编辑类、dontAsk 拒绝未预批准(顺序在 autoAllowTools 之后,尊重会话级 always);EDIT_TOOLS 提为模块常量,只读硬拦截保持最前(bypass 也拦)。npm test 120/120。

- 2026-08-19 v0.9.38(未 commit):**image/video/audio/model 四大媒体板块合并为「创作」板块**(按 AI创作平台功能规划方案.md 工具集单入口定位,用户遇提问跳过,按推荐方案做彻底合并)。架构:①会话 kind 统一为 'media'(migrations 登记 0.9.38 版本化迁移,image/video/audio/model→media,幂等;代码全面兼容旧 kind:MEDIA_KINDS=media+四类旧值,state.sectionOfKind 统一映射),生成类型不再随 kind,改由所选模型的 model_type 决定——同一会话可先生成图再拿它做参考图生成视频(为 md 跨模态链路打底);②顶栏 4 按钮→「创作」单按钮,侧栏加工坊筛选 chips(全部/图片/视频/音频/3D,state.mediaShop,filters 按 boardOf 过滤会话;新建会话按工坊预选首模型);③模型下拉 media 板块按全部四类模型列出,optgroup 按「Key · 类型」分组;④body 挂 board-<type> class(updateTopbarForSession 时 setBoardClass,异步 ensureGroups 后应用+复核 activeSid 防乱序),附件按钮仅 board-image/video 显示(CSS body.sec-media:not(.board-image):not(.board-video));⑤关键新件:state.GroupsCache(Kuro modelGroups 全局缓存,populateModelSelects 每次刷新/ensureGroups 懒加载兜底)、state.boardOf(keyId,model,kind,board)(分组→board 戳→旧 kind 三级兜底)、aigc.resolveBoard(主进程 aigc:send 注入 keys.modelType 定建单端点,查不到且旧 kind 是 media 时拦截报错让重选模型);⑥**meta.board 戳**:迁移时盖原 kind、sess:create/sess:setModel 盖模型类型、migrations 新增 repairMediaBoard 自愈(每次启动给 kind=media 且戳缺失/无效的会话按 modelGroups 补盖)——解决「modelGroups 被刷新失败清空后旧版按 kind 建单仍可用、新版无从解析」的回归缺口,冒烟实测真实数据 mediaBoardStamped:4;⑦任务卡片产物渲染 mediaHtml 改按文件扩展名选 img/video/audio/文件卡(不再按会话 kind——跨类型会话图片视频并存);⑧attachments→media_ref 的 MCP 辅助分析(aux-models.js/keys.js 的 aux image/audio/video/model)与板块**无关**,未动。npm test 124/124;electron 冒烟通过(migrations.log 迁移 ok+repair 补戳,渲染端无报错);坑:测渲染 ESM 模块语法须拷成 .mjs 再 node --check;asar 内路径是反斜杠,extractFile 要用 'src\\main\\x.js' 形式。dist\Drafter Setup 0.9.38.exe 已构建(含全部改动,check-asar.js 验证过)。

- 2026-08-19 v0.10.0(未 commit):**无限画布 v1 + 素材库**(AI创作平台规划 md 的两个 MVP 模块)。调研(WebSearch 在本环境不可用——Bedrock 端点不支持该工具类型,改用 WebFetch 直连 GitHub 核实):引擎选 **Drawflow**(jerosoler, 6.1k★, MIT, 0.0.60, vanilla 零依赖,DOM 节点原生放 video/audio 播放器;litegraph.js 是 ComfyUI 同款但 canvas 渲染放媒体控件难,备选);功能参照 ComfyUI(类型槽连线校验/节点结果画廊)与 CushyStudio(fan-out 并排)。架构:①主进程 src/main/canvases.js——画布单文件 userData/canvases/<id>.json(Drawflow export graph+节点 data:prompt/models/tasks),saveUpload 参考图落 <id>/assets,listAssets 双源聚合(媒体会话 JSONL aigc_task done + 画布节点 tasks,existsSync 剔除,时间倒序),**patchTask 终态补丁**(用户切走/关窗后任务完成仍写回画布 JSON);②main.js 新 IPC canvas:list/create/load/save/delete/saveUpload、assets:list、**aigc:exec(画布非会话执行:读磁盘 refFiles 限 AIGC_DIR+canvases 目录防任意读取,20MB 守卫,resolveBoard 按模型 model_type 定端点)**;③渲染端 canvas.js——6 节点(文本/参考图/图/视/音/3D),text→prompt 槽接管(只读展示+实时同步),image→参考图/首帧槽,**fan-out:节点内模型多选 chips,每模型独立任务,结果翻页+「采用」供下游取用**,任务历史翻页,防抖自动保存,侧栏画布列表(复用 session-item 样式,行内 input 改名——**window.prompt 在本 Electron 不可用**);assets.js——素材网格(aigc:// 缩略),「用作参考图」读盘塞创作会话附件并跳板块(md 图→视频主链路);④顶栏五板块 Code/Chat/创作/画布/素材,素材板块全宽隐藏侧栏,画布板块侧栏=画布列表;⑤板块切换非会话板块(canvas/assets)跳过会话挑选流程(新增 NON_SESSION_SECTIONS)。测试 129/129(canvases.test.js 5 例);**CDP 冒烟驱动法**(electron --remote-debugging-port=9222 + Node 内置 WebSocket,.claude-ui/smoke-canvas.js)14/14 步通过,真实 Key 数据下图片节点 29 个模型 chips、素材网格渲染真实产物、零渲染错误。坑:单实例锁——机器上装着运行中的 Drafter 时 dev 实例必退,要用 DRAFTER_USERDATA 隔离目录起冒烟实例;Edit 多行替换注意别吞相邻 test() 头。dist\Drafter Setup 0.10.0.exe 已构建,check-asar.js 14 文件全过(含 drawflow)。
- 2026-08-19 v0.10.0 同批(未 commit):修「输入框有时点击无反应、无法输入」。根因=流式期间渲染主线程被打满,点击事件排队得不到处理:①appendThinking 此前不合帧,每个 thinking delta 直接 textContent +=(几万字思考=几万次 DOM 写+O(n²) 拼接);②scheduleAssistantRender 对后台隐藏会话照样每 tick 全量 marked.parse;③超长消息 80ms 帧间隔本身不够。修复(chat.js):thinking 并入 80ms 合帧(thinkBuf/thinkEl/thinkOpen,renderAssistantNow 统一出口);非激活会话/document.hidden 跳过渲染只标 renderDirty,setActiveSession 与 visibilitychange 补渲;帧间隔按缓冲长度自适应 80ms→1.2s。content_block_start(text/thinking)与 message_stop 负责关思考块(thinkOpen=false+去 live)。npm test 129/129。

- 2026-08-19 **v0.10.0 已发布**:commit 5a48fad(五大板块+画布+素材,26 files)+ e8e9e8b(发布脚本)已 push;tag v0.10.0;GitHub Release v0.10.0(id 372863428)已上传 exe(147MB)+blockmap+latest.yml(electron-updater 可自动更新);发布脚本 .claude-ui/release-0.10.0.js(已入库);DEVLOG.md 补齐 v0.9.36-v0.10.0 四版记录。运行中 App 需重启生效。md 剩余路线分两步:本轮先做 MVP 收尾(LLM 文本节点/画布模板/fork 导入导出,v0.10.1),片场(剧本→分镜,大模块)单独做。

- 2026-08-19 **v0.10.1 已完成(画布 MVP 收尾)但 GitHub Release 未发**:commit 2bc2958 已 push + tag v0.10.1 已 push;dist\Drafter Setup 0.10.1.exe 已构建(check-asar 全过)。内容:①文本生成节点 llmtext(画布「文本生成」节点,chat 模型多选 fan-out 走 /v1/chat/completions,新主进程模块 src/main/llmtext.js,采用版本文本喂下游 prompt 槽——prompt 槽文本源从纯文本节点扩展到 llmtext);②画布模板(canvases.js sanitizeGraph 剥离 tasks/results/file,模板存 canvases/templates/,renderer 播种 2 预置,▦模板菜单存/用/删);③画布 fork 导出/导入(exportPayload/importPayload 结构校验,导出剥离历史,导入自动加「(副本)」)。npm test 136/136(llmtext.test.js 5 例),CDP 冒烟 17/17(新增 llmtext/模板菜单/从模板建画布 3 节点 2 连线步骤)。
- 2026-08-19 ⚠ **并行会话交织事件**:另一会话(v0.10.2 极速问答)与我同时在工作树干活——它的 commit 7af68ed(消息误标「v0.10.1 四大媒体板块合并」)卷走了我的 canvases.js+llmtext.js,我的 commit 2bc2958 卷走了它的 sessions.js(FAST_CHAT_SYSTEM_PROMPT)+main.js(sess:setChatMode)。已发布的 v0.10.1 安装包里含它的后端半成品:chat 会话默认进极速问答(零工具/极简提示),但它的切换 UI(src/index.html/app.js/chat.js/styles.css)还未提交,装包用户没有开关。协调结论:不回滚(fast-chat 后端自洽、chat 板块本就是纯问答语义,resume/锚点兼容),**v0.10.1 的 GitHub Release 暂缓不发**(tag 已推但 electron-updater 看不到 Release 就不会推送给用户),等并行会话把 v0.10.2 UI 做完后一起发 v0.10.2(内容含全部 v0.10.1)。教训:并行会话同树作业时,提交前先看 git status 里有没有不属于自己的改动,分文件 add 而不是 git add -A。
- 2026-08-19 v0.10.2 极速问答已完成(未 commit):①chat 会话默认 fast——fastChatOverrides(sessions.js 纯函数可测):tools:[]/settingSources:[]/mcpServers:{}/strictMcpConfig/Drafter 极简系统提示(Gem append 仍拼入),首轮输入实测 26k→2-4k tokens;②⚡/🛠 切换钮(composer-toolbar,chat-only CSS 类),sess:setChatMode 走 needRestart/stop+start(resume 保上下文),存量 chat 首次激活一次性提示(settings.fastChatHinted);③fast 下文本附件改内联注入(≤100KB,超限/失败在附件块注明);④跨会话共享提示缓存:新会话盖戳 meta.staticPrompt(设置项 sharedPromptCache 默认开),preset 分支加 excludeDynamicSections,既存会话不盖戳不中途换提示;⑤TTFT 提示 curAction「已发送,等待响应…」首个流事件清除(result 兜底);⑥侧栏 refreshList 200ms 尾沿防抖;⑦品牌出清:package description/README/编辑器外部修改提示/Key 页登录态文案。test/chat-fast.test.js 7 例,npm test 143/143。注意:v0.10.1 Release 仍暂缓,与 v0.10.2 一起发。
- 2026-08-19 实测依据(Chat 慢根因,存查):kimi-k3 经 Kuro 网关,首轮输入 25.7k-31.6k tokens(系统提示+工具 schema+环境注入),缓存读仅 1536;追问 5 分钟内缓存命中(27.5k 缓存读);解码 ~20-25 tok/s;num_turns=1 零工具调用——慢在单请求体量+冷缓存+网关吞吐,不在 agent 循环。SDK v0.3.218 支持 tools:[]/settingSources:[](隔离模式)/systemPrompt 字符串或 string[]+SYSTEM_PROMPT_DYNAMIC_BOUNDARY/excludeDynamicSections/strictMcpConfig。
- 2026-08-19 外部框架调研结论(存查):Codex CLI 系统提示仅 ~1200-1400 词+工具面极小(rg/apply_patch/plan)+强简洁指令;Gemini CLI 静态核心+动态段参数化拼接+历史压缩一等功能(getCompressionPrompt);Aider 前缀稳定化。极速问答=Codex 思路;excludeDynamicSections=Aider/前缀稳定思路,SDK 原生支持。
- 2026-08-19 v0.10.2 已发布:commit 54bec72 已 push;GitHub Release v0.10.2(id 372906966)已上传 exe(147MB)+blockmap+latest.yml(check-asar-0.10.2 全过)。发布脚本 .claude-ui/release-0.10.2.js(未入库)。Release 说明里明确标注「包含 v0.10.0/v0.10.1 全部内容,这两版未单独发 Release」;远程空壳 tag v0.10.1 已删(它永不会有 Release,留着会误导)。v0.10.0 tag(有 Release)不动。
- 2026-08-19 v0.10.3(未 commit):**修复「Chat 比网页版慢 3-5 倍」真根因**——不是 v0.10.2 查的输入体量,而是 **Kimi k3 类混合推理模型在 /v1/messages 下默认开启扩展思考**。实测(Kuro 网关 kimi-k3,同 prompt 探针):默认 thinking_tokens 占输出 70%+、~20 tok/s;`thinking:{type:'disabled'}` 后 ~50-70 tok/s 与网页版同量级;`output_config.effort` low/medium/high 均压到 ~20 thinking tokens 也同速,但 xhigh/max 回升 400-600;`budget_tokens:0/1` 被网关忽略(反思考更多)。修复(sessions.js):①fastChatOverrides 加 `thinking:{type:'disabled'}`(SDK 0.3.218 ThinkingConfig 原生支持,映射 CLI `--thinking disabled`);②fast 模式跳过会话级 effort(effort 是思考深度调节,fast 固定无思考,防 --effort max 重新放大推理);③FAST_CHAT_SYSTEM_PROMPT 加「不要长思考,直接作答」提示层双保险。claude-opus-5 回归验证接受 thinking:disabled 正常。test/chat-fast.test.js 补 2 断言,npm test 143/143。注意:正在运行的 App 需重启进程生效;既有 fast 会话的 query 已带旧配置,新 send 重启 query 后才带新配置(空闲会话下次 send 自动重启,忙碌会话回合结束后 needRestart)。
- 2026-08-24 v0.10.3 同批(未 commit):设置面板加「更新」区——🔄 检查更新按钮 + 状态行(当前版本/仓库版本/是否有新版)。updater.js 新增 checkRepoVersion:走 github.com/891458249/Drafter/releases/latest 的 302 重定向取 tag(**不用 api.github.com**——未鉴权 60 次/小时/IP,共享出口 IP 极易撞 403 限流,302 重定向不耗配额),compareSemver 三段比较,hasUpdate 时渲染端再触发 electron-updater 下载(dev 静默失败)。electron-updater 改惰性 require(顶层 require 在非 Electron 单测环境崩:ElectronAppAdapter getVersion undefined)。test/updater.test.js 3 例,npm test 146/146。
- 2026-08-24 v0.11.2(未 commit):更新区补「更新」动作——此前只有检查没有更新按键(设计上靠 electron-updater 后台静默下载+顶栏 chip 提示,设置面板断了)。现在检测到新版显式给:①「⬇ 立即更新」按钮(仅 res.packaged 时显示,dev 环境 electron-updater 缺 dev-app-update.yml 无法下载)——点击触发 updateCheck,下载进度经 'update:status' 的 downloading/downloaded 事件回灌按钮文本(下载中… N% → 🔁 已就绪·重启安装,install 阶段点击调 updateInstall);②「🔗 Release 页」兜底按钮(dev 或自动下载失败 idle 事件时显示,openExternal 打开 Release 页手动下载)。checkRepoVersion 返回加 packaged 标记(app.isPackaged)。settingsUpdateProgress 挂在 update:status 监听首部(与顶栏 chip 同一事件源)。npm test 146/146。
- 2026-08-26 v0.11.2 已发布:commit 4a0734b 已 push;GitHub Release v0.11.2(id 376663792)已上传 exe(181MB)+blockmap+latest.yml。发布脚本 .claude-ui/release-0.11.2.js(未入库,同前)。端到端验证:v0.11.1(packaged)检查 → hasUpdate:true → 302 重定向正确指向 v0.11.2。用户将在 v0.11.1 上验证完整更新链路(检查→立即更新→下载→重启安装)。
- 2026-08-25 **启动 harness 合并**(deepseek-harness 并入 Drafter):计划文件 ~/.claude/plans/zany-inventing-panda.md。路线=方案 C 宿主合并:harness 作运行时引擎进 Drafter 主进程+React Web 前端经 file://+IPC 桥加载,Drafter 独有(媒体/画布/素材/Gem/皮肤/托盘/更新)保留共存。关键约束:harness SDK 无审批流+无轮次取消(官方 README 明示)→ 不能走 SDK 路线,必须走 file://+IPC 桥(官方 RFC 2026-07-19-gui-layering-and-rpc-protocol 预留,继承 AbstractApiClient 重写 doFetch)。Phase 0 已完成:vendor/deepseek-harness(65MB,0.1.1-rc.2,UPSTREAM.txt pin)+pnpm install(4m39s)+build 绿+dsh web 起服务 OK。Node v24.18.0/pnpm 11.7.0。坑:WebFetch 抓 GitHub README 会被代理投毒(返回假内容),须用 curl+GitHub API+tarball 核实。
- 2026-08-25 **Phase 1(Electron IPC 桥)核心链路已通**。实现:①`src/main/harness/harness-bridge.js`——主进程内 `boot()` harness 运行时(loadProfile('web')+composeEntries 合成 bundle patches+Drafter overlay 禁 webserver/web-runtime/client-hmr/code-runtime),`toFetchHandler(ctx.apiProxy)` 提供 RPC;自绘 `drafterWebServerStubPlugin`(不监听端口的 webServer 注册表存根,含 renderIndex 复刻)满足 client-modules 的 inject;`renderHarnessIndex()` 把 dist/index.html 经 webServer.renderIndex 注入 __DSH_BOOT__ + 改写绝对路径为 file:// + 头部注入 transport 安装 inline module。②`src/harness/preload.js`——只暴露「同步纯数据」原语 `__DRAFTER_IPC_RAW__`(fetch/loadBundle/openSse/paths),**不构造 Response/ReadableStream**(实测 contextBridge 跨世界函数返回的 Web API 对象构造器错位,res.text is not a function)。③`src/harness/ipc-client-entry.js`(tsdown 打包成 dist/ipc-client-entry.mjs,自包含含 zod)——主世界继承官方 `AbstractApiClient` 实现 `IpcApiClient`,doFetch 走 IPC invoke、openMux/openHost 走「channelId + webContents.send」SSE 桥(**不用 MessagePort**:contextBridge 不能 transfer port),installDshTransport() 由 index.html 的 inline module 调。④main.js 注册 harness IPC。**实测通过**:Electron(Node 20)+ Node 24 双环境 boot 成功(apiProxy/webServer/clientModules/llm/sessions/agents/tools/settings/credentials 全就绪)、UI 完整渲染(新会话/工作区/设置)、`session.list` 经完整 IPC 桥返回 `ok:true`。npm test 146/146 无回归。
- 2026-08-25 **Phase 1 踩坑记录**(关键):①Node 20 缺 node:sqlite/createZstdDecompress/stripTypeScriptTypes——sqlite 是 import type+openAt:never 惰性(保留 session-query-sqlite 行,它提供 ctx.sessionQuery 服务,apiproxy 依赖),zstd 给 session-persistence-jsonl 的 built lib/index.js(tsdown bundle,非 types/)打补丁把具名 import 改惰性 import * as _zlib,code-runtime 直接禁用(run_code 工具 Phase 1 不用)。②Node 20 的 loader.internal 为 undefined(fromInternal 仅支持 22+)→ 手搓多锚点 shim(createRequire 以 web-app/base/cli 三个 package.json 为锚点轮询 resolve),Node 24 则包装原 internal 加失败回退。③patch 的 config 是整值覆盖(vendor/include),改 config 必须补回必填字段(session-persistence-jsonl 的 root)。④渲染进程浏览器不能解析裸包名(zod)→ AbstractApiClient 必须 tsdown 打成自包含 bundle。⑤cordis 动态运行时(cordis-client-runner/ui-cordis)的 RPC 不在 apiproxy RpcMethodMap(404)→ overlay 禁用。冒烟脚本:.claude-ui/smoke-harness.js(remote-debugging-port=9223 + DRAFTER_USERDATA 隔离)。
- 2026-08-25 **Phase 2(模型与凭据对接)核心通了**:`src/main/harness/keys-bridge.js` 把 Drafter 的 `settings.apiKeys[]` 映射为 harness `llm-pi-ai.providers` 路由(api 判定 anthropic-messages/openai-completions;Kuro/anthropic 协议走 anthropic-messages)。关键坑:①harness 凭据引用名必须匹配 POSIX 环境变量格式 `/^[A-Za-z_][A-Za-z0-9_]*$/`(`credentialRefOf` 把 key.id 的 `-` 转 `_`、加 `DRAFTER_KEY_` 前缀);②写错格式的 ref 会残留在 `$DSH_HOME/.credentials.yaml` 导致下次 boot 崩(清理后恢复);③同步链路:credentials.set(明文)+ settings.replace('llm-pi-ai', {providers}) + agentDefaultModel.saveSelection({provider,model})。实测:store 种一个 Kuro 测试 Key → syncKeysToHarness → `ctx.llm.listProviders()` 返回 `["deepseek-official","k_test_kuro"]`。已挂到 harness-bridge boot 后自动同步 + 暴露 `resyncKeys()` 供 Key 变更重同步。npm test 146/146 无回归。
- 2026-08-25 **Phase 0-2 已提交**:commit cb59b7f(7932 files,+922k 行,含 vendor/deepseek-harness 全量源码+lib 构建产物)。.gitignore 排除 vendor/**/node_modules、apps/web/dist、tsbuildinfo;lib/ 入库因含 Node 20 兼容补丁(zstd 惰性化)。未 push。
- 2026-08-25 **Phase 3-5 已提交**(commit 紧随 cb59b7f):①Phase 3 权限——permission-bridge.js 把 Drafter 5 档权限模式映射为 harness sandbox×approval 预设表(overlay 替换 permission 行 config,实测 optionOf('default') 返回「默认」);②Phase 4 Gem/项目组——gems-bridge.js 把 Gem 渲染为 persona 行的 agent preset YAML + systemPrompt.section,项目组共享记忆映射为 systemPrompt section;③Phase 5 板块共存——新增第六板块「Harness」(index.html data-sec=harness + #harness-root 用 **webview 而非 iframe**(webview 才能挂 harness 专属 preload),renderer/harness.js 管 boot+加载,styles.css 板块显隐,app.js SECTIONS/NON_SESSION_SECTIONS 收编)。冒烟 smoke-harness-section.js:完整 Drafter 切 Harness 板块,webview 内 transport+boot 全就绪,UI 完整渲染。npm test 146/146 无回归。坑:contextBridge 跨世界函数返回的 Web API 对象构造器错位(preload 只暴露纯数据原语,Response/ReadableStream 在主世界 bundle 里构造)。
- 2026-08-25 **Phase 6(数据目录归拢)完成**:harness-bridge 在 import 任何 harness 模块前设 `process.env.DSH_HOME = <userData>/harness`(resolveDshHome 在模块加载时读 env),harness 的 profiles/settings.yaml/storages/sessions 全部落到 Drafter userData 下(实测 resolveDshHome() 返回 userData 路径)。坑:loadProfile 只建 package.json/cordis.patch.yml/pnpm-workspace.yaml,不建 cordis.yml(官方 CLI 的 prepareProfile 才写),bridge 里补了「不存在则写空 entry 列表」。另:cordis.yml 的 pnpm-workspace.yaml 模板引用 profiles/node_modules,healProfilesModuleFallback 已在 boot 前跑。
- 2026-08-25 **Phase 7(打包)进行中 + Electron 38 升级**:Electron 33 → 38(Node 20 → 22.22.0,支持 require(esm),解决 harness 的 Node 22+ 硬依赖——schemastery 的 CJS 版 require 纯 ESM 的 cosmokit)。package.json build.files 收编 vendor 的 package.json/lib/cordis.patch.yml/apps/web/dist/**vendor-deps**(第三方依赖裁剪自 .pnpm:29 个包 52MB,含 zod/fflate/yaml/pi-ai/opentelemetry);asarUnpack 把 vendor lib/dist/vendor-deps 解出 asar(ESM import 读不到 asar 虚拟文件系统);harness-bridge 注册全局解析:ESM resolve hook(resolve-hook.mjs,@deepseek-ai/* + 第三方包映射到物理路径)+ CJS require hook(Module._resolveFilename)。**坑与决策记录**:①harness 的 cordis.patch.yml 含 `!!js` 表达式,Loader 在条目激活时用 interpolate(ctx) 求值,不是 YAML 解析时——我们用 js-yaml + entryListSchema 解析成 __jsExpr 占位符即可;②loadProfile 的 resolveBundleDir 在打包态找不到 bundle(没 pnpm 符号链接)→ 改为直接读两个 bundle 的 cordis.patch.yml;③fflate 的 exports 是嵌套对象(node.import.default),resolve 要优先 node 条件;④`ERR_REQUIRE_ESM` 在 Electron 33(Node 20)是死结,Electron 38(Node 22)才支持;⑤**单实例锁坑**:打包态冒烟会被本机正在运行的 Drafter 抢锁直接退出,main.js 加了 DRAFTER_ALLOW_MULTI_INSTANCE=1 开关。冒烟脚本:.claude-ui/smoke-packaged.js + probe-packaged-deep3.js(CDP 驱动 dist/win-unpacked/Drafter.exe,ws 从 harness pnpm store 借)。已知卡点:打包态还剩子路径导出(@earendil-works/pi-ai/providers/all)、原生模块(koffi)、平台特定包(landlock)等零星解析问题;开发态(Electron 38 + 源码目录)已完整跑通。npm test 146/146 无回归。
- 2026-08-25 **Phase 7 打包态收尾完成**(commit 81db245):**打包后的 dist/win-unpacked/Drafter.exe 完整跑通 harness**。解决的解析问题:①vendor-deps 补齐 readdirp/typebox/partial-json/@opentelemetry 全套(含 semantic-conventions/otlp-transformer/otlp-exporter-base/exporter-logs-otlp-http)/sharp(含 @img/sharp-win32-x64 原生 .node + libvips dll)/koffi(含 @koromix/koffi-win32-x64);②resolve-hook.mjs 支持 exports 子路径通配符('./api/*' → './dist/api/*.js')+ node 优先条件;③CJS hook 支持子路径 require(@img/sharp-win32-x64/sharp.node);④native/landlock-run 纳入索引与打包;⑤禁用了三个非核心插件:attachment-local(sharp 图片处理,Drafter 走自己的 aigc)、session-telemetry-otel(遥测)、web-startup(CLI 参数解析),并给 attachments 服务最小 stub(apiproxy inject 需要)。实测:打包态切 Harness 板块,harness 前端在 webview 里完整渲染(transport+boot+ipcRaw 全 true),RPC session.list 经完整 IPC 桥返回 ok:true。npm test 146/146 无回归。冒烟:.claude-ui/probe-packaged-full.js(完整验证:boot+点击+webview 内部状态+真 RPC)。
- 2026-08-26 **v0.11.0 已发布**:commit 07b7188(发布脚本)已 push;GitHub Release v0.11.0(id 376624592)已创建并上传 Drafter Setup 0.11.0.exe(181MB,比上版大——内置 harness 运行时)+blockmap+latest.yml(electron-updater 可自动更新);tag v0.11.0 指向 07b7188。发布脚本 .claude-ui/release-0.11.0.js(已入库)。内容:Harness 板块(实验)+ Electron 38 升级(Node 22)。**注意:打包构建的坑——pnpm 的 vendor-deps 里符号链接(Windows junction)cpSync 不自动解引用,7za 压缩死链会失败;用 fs.cpSync(..., {dereference:true}) 或先 realpathSync 再复制。**
- 2026-08-26 **v0.11.1 已发布**(commit 0d33467,Release id 376648662):修 Harness 板块宿主能力——①目录选择器:harness 的 directory-picker-native 走 Windows IFileOpenDialog 子进程(koffi COM),Electron 主进程跑不起来;禁掉 directory-picker 行,prepare 里 ctx.provide('directoryPicker') 用 Electron 自己的 dialog.showOpenDialog 顶替。②设置可用性:harness 的 connection 插件用 isLoopbackHostname(location.hostname) 判断 settings 可用性;file:// 下 hostname 是空字符串被误判非 loopback → settings mirror 用 memory 持久化 → 'settings are unavailable in this browser';修 vendor/deepseek-harness/packages/client/connection/lib/client.js 的 isLoopbackHostname(空 hostname 视为 loopback)。实测:打包态 settings.describe 返回 ok:true + writable:true。③已知未修:插件清单/目标跟踪/消息反馈等走 ctx.remote(Typert RPC,独立于 apiproxy 的 /api/* 传输),我们的 IPC 桥只转了 apiproxy;这些 UI 显示「暂时无法读取插件」,不影响会话主链路。**注意:harness 的 Typert RPC(api-remotes)是独立于 apiproxy 的第二套 RPC 传输,要支持需另起一路桥接。**
- 2026-08-25 **Phase 6(数据目录归拢)完成**:harness-bridge 在 import 任何 harness 模块前设 `process.env.DSH_HOME = <userData>/harness`(resolveDshHome 在模块加载时读 env),harness 的 profiles/settings.yaml/storages/sessions 全部落到 Drafter userData 下(实测 resolveDshHome() 返回 userData 路径)。坑:loadProfile 只建 package.json/cordis.patch.yml/pnpm-workspace.yaml,不建 cordis.yml(官方 CLI 的 prepareProfile 才写),bridge 里补了「不存在则写空 entry 列表」。另:cordis.yml 的 pnpm-workspace.yaml 模板引用 profiles/node_modules,healProfilesModuleFallback 已在 boot 前跑。
- 2026-08-25 **Phase 7(打包)进行中**:package.json build.files 加了 vendor/deepseek-harness 的 package.json/lib/cordis.patch.yml/apps/cli/config/apps/web/dist;electron-builder --dir 打出的 asar 含 3146 个 vendor 文件(13/13 关键文件全在,check-asar-harness.js 验证)。坑:asar.listPackage 的路径分隔符是反斜杠,匹配前要先归一;index.html 改动后必须重新打包,否则 asar 里是旧版(冒烟曾在旧包上白等)。冒烟脚本:.claude-ui/smoke-packaged.js(CDP 驱动 dist/win-unpacked/Drafter.exe,ws 从 harness pnpm store 借,NODE_PATH 指向 .pnpm/ws@8.21.0)。
- 2026-08-26 v0.11.3 已发布:commit 6ccaa75(仅版本 bump,无功能变更)已 push;GitHub Release v0.11.3(id 376673371)已上传 exe(181MB)+blockmap+latest.yml。发布脚本 .claude-ui/release-0.11.3.js(未入库)。用途:验证 v0.11.2 的自动更新链路(检查→立即更新→下载→重启安装);端到端模拟 v0.11.2 检测 → hasUpdate:true → 302 正确指向 v0.11.3。
- 2026-08-26 v0.11.3 自动下载 404 根因:electron-updater 按 latest.yml 的 url(连字符名 Drafter-Setup-X.exe)拼下载地址,而发布脚本用空格名(Drafter Setup X.exe)上传,GitHub 把空格净化成点(Drafter.Setup.X.exe)→ 下载 404。**老版本自动更新从来没成功过**(v0.9.x 起就这名不匹配,用户之前全是手动下载安装包更新)。修复:exe/blockmap 用连字符名重传(空格名上传 GitHub 净化成点,连字符名原样保留);release-0.11.3.js 改 ASSETS 为 {local, remote} 分开,上传后校验 latest.yml 的 url 可达。**今后发布脚本一律用连字符名上传(与 latest.yml 一致)**。
- 2026-08-26 **v0.11.4 已发布**(commit 903022a,Release id 376702440,tag v0.11.4,exe+blockmap+latest.yml 已上传,下载 URL 校验 302 OK):修「切换到另一个 API Key 的模型后发送报 403 模型未配置」。根因:会话凭据(ANTHROPIC_AUTH_TOKEN/API_KEY/BASE_URL)只在 query start() 时按 meta.keyId 注入一次;旧版 setModel 跨 Key 切换只调 q.setModel 改模型名不重启 query → 旧 Key 的 token 打到新 Key 网关被字面校验拒(403)。修复:①sessions.js setModel 检测 keyId 变化时复用 setGem/addDir 的 needRestart 模式重启 query 换凭据(回合中标记/空闲立即 stop+start,resume 保上下文);同 Key 仍走 q.setModel 不重启。②渲染端 input.js model-sel onchange:sess:setModel 返回 false(守卫拦截)时不再乐观写 meta,下拉回滚+提示。test/setmodel-restart.test.js 5 例,npm test 151/151。
- 2026-08-26 **v0.11.5 已发布**(commit e0c96e9,Release id 376860132,tag v0.11.5,exe+blockmap+latest.yml 已上传,下载 URL 校验 302 OK):修「Kimi 等 baseUrl 带 /v1 的网关 Key 聊天全 404,表现为无法识别 Key」。根因:buildEnv 把 baseUrl(如 Kimi 预设 https://api.kimi.com/coding/v1)原样注入 ANTHROPIC_BASE_URL,claude.exe 再拼 /v1/messages → /coding/v1/v1/messages 404(模型识别走 keys.js apiRoot 本就剥 /v1,故识别正常)。修复:buildEnv 改用 keys.apiRoot 归一(keys.js 导出复用,空值仍归位官方);test/keys.test.js 加 apiRoot 回归用例。另修测试基建卡死(v0.11.4 起既有):setmodel-restart.test.js 的 mgr.create() 会 fire-and-forget 真调 Session.start() spawn claude.exe,子进程占住事件循环——测试全过但进程不退,整套 npm test 卡死;修复:sessions.js 导出 Session,测试里 stub 原型 start(教训:create() 自带真启动,会话类测试必须拦原型)。修后 npm test 152/152(1.9s)。版本 bump 0.11.5 + 发布脚本 .claude-ui/release-0.11.5.js。
- 2026-08-26 **v0.11.6 已发布**(commit 1db9cb2,Release id 376884234,tag v0.11.6,exe+blockmap+latest.yml 已上传,下载 URL 校验 302 OK):修「切到 Kimi 等非 Kuro Key 的模型仍报 403 模型未配置」。根因:~/.claude/settings.json 的 env(ANTHROPIC_AUTH_TOKEN/BASE_URL,本机钉死 Kuro)优先级**高于** buildEnv 按会话 Key 注入的进程凭据——所有会话实际都打到 Kuro,Kuro 模型碰巧可用,其它 Key 的模型被网关拒(403)。v0.11.4 的重启换凭据对此无效(换进程 env 仍被 settings.json 盖)。修复:sessions.js query 的 settingSources 去掉 user(保留 project/local,项目级权限照常)。**教训:settings.json env > 进程 env;app 多 Key 体系必须隔离用户级 CLI 配置**。真实 claude.exe 端到端验证:含 user → 403 复现;去 user → k3 正常。副作用:未配 Key 时隐式回退用户级凭据失效;~/.claude/CLAUDE.md 不再注入。npm test 152/152。
- 2026-08-26 **v0.11.7 已发布**(commit 3a4d2f1 已 push + tag;Release id 376913208,exe 181MB+blockmap+latest.yml 已上传,下载 URL 302 校验 OK;发布脚本 .claude-ui/release-0.11.7.js 未入库)。内容::修 Harness 板块「新会话/选择工作区」点击无反应 + Typert 桥接通**。根因有二:①v0.11.1 禁了 directory-picker(-auto)行,但 -auto 本来会同时挂 host 后端与 client 界面(ui-directory-picker-browse/native 占用 ui-workspace 的 directoryFlow 槽);槽位一空,WorkspacePickFlow 的 popover 为空,「锚点手势直接不显示任何内容」→ 选择工作区点击死;新会话在无工作区时本就只 clear 选择(看起来像没反应)。②Typert RPC(ctx.remote.*,goals/pluginInventory/messageFeedback 等)走 connection 的 /api 复合 FetchHandler(gateway interceptor+apiproxy 回落),我们 v0.11.0 起直连 toFetchHandler(apiProxy),Typert endpoint 全 404(设置→插件清单读不出的根因)。修复(harness-bridge.js):①overlay 新增 insert 行直接挂 browse 双面板(@deepseek-ai/dsh-host-directory-picker-browse 纯 Node fs 列目录 + @deepseek-ai/dsh-client-ui-directory-picker-browse;api-proxy boot 期间就 inject directoryPicker,所以必须进树不能 boot 后挂),删掉 v0.11.1 的 Electron dialog native provide;②boot 后 apiFetchHandler 换成 connection.createSharedFetchHandler('/api', 纯 apiproxy 回落),回落分支不能照抄官方 426(我们的 SSE 走 fetch 不走 WebSocket)。冒烟:.claude-ui/smoke-harness-typert.js(主进程 6 断言:Typert pluginInventory/list 通、browse listDirectory 通、loader 两行在、clientModules 解析到 surface、未认领 404)+ smoke-harness-picker-e2e.js(完整前端:__DSH_BOOT__ 39 行含 picker surface,点击选择工作区弹出「选择工作区目录」对话框,0 console 错误)。npm test 152/152。坑:Electron 裸跑冒烟的 userData 是 Roaming\Electron\harness,用完要清。已知小限制:Typert 调用的 AbortSignal 不过 IPC(invoke 不可取消),断开取消不传播。
- 2026-08-26 **v0.11.8 已发布**(commit e7a1212 + tag,Release id 376964369,exe+blockmap+latest.yml 已上传,302 校验 OK;发布脚本 .claude-ui/release-0.11.8.js 未入库):Harness 插件列表「启用/停用」开关。根因:上游 pluginInventory 是只读投影,官方启停靠手改 profile cordis.patch.yml,而 Drafter 内存合成树没有可写用户补丁层。实现:①新模块 src/main/harness/plugin-control.js——SRC 模式 Typert Remote 服务 pluginControl/setEnabled(plain JS 手动应用 @Remote:Remote(exportName)(fn, {name, addInitializer: fn => fn.call(Object.create(proto))}) 把标记写进 WeakMap;运行态 entry.update({disabled}) 切换;持久化写 $DSH_HOME/profiles/web/cordis.patch.yml 用户补丁层,原子写);②harness-bridge boot 把用户补丁层追加为最后一层 patches(后层优先),prepare 挂载该服务;③vendor ui-settings-plugin-inventory 源码卡片详情加开关按钮(经 __DSH_TRANSPORT__.fetch 走 /api 复合通道),tsdown 重建。**坑**:运行态条目 id 是 include:<rowId>(树路径前缀),loader.store 只键顶层条目,要遍历 entries() 匹配;补丁层按 YAML 行 id(options.id,无前缀)定位。冒烟:smoke-harness-typert.js 14 断言(含开关双向+重启持久化)+ smoke-harness-toggle-e2e.js(真实 UI 点击)全过;vendor 包测试 8/8;npm test 152/152。vendor 源码级修改首次走「改 src + pnpm bundle 重建 lib」路线(此前都是直接 patch lib)。
- 2026-08-26 **v0.11.9 已发布**(commit 9ba6dd8 + tag,Release id 377011201,exe+blockmap+latest.yml,302 校验 OK;发布脚本 .claude-ui/release-0.11.9.js 未入库):修「插件列表停用 include 后 /api 全 404」。根因:插件列表第一张卡片 include 是 cordis 根载体(运行态条目 id 无前缀 'include',cordis:include 组),承载整棵插件树——v0.11.8 的开关没防护,运行态 entry.update({disabled:true}) 等价拆树,apiproxy 随之 dispose,fallback 对所有路径 404(模型设置 /api/llm.providers 404 即此);补丁行持久化又造成「文件说停用、boot 实际启用」的漂移(boot 时根 include 的数据行不参与补丁匹配,该行对 boot 无效)。修复:①pluginControl.setEnabled 拒绝根载体(cordis:include 或 id 无 ':')与插件组(options.group);②loadUserPatches 读取时剔除补丁层里的 include 行并回写修复(自愈,用户的坏文件已修);③UI 对载体卡片不渲染启停按钮。冒烟 18 断言(含拒绝后 apiproxy 健康、补丁修复、重启保持)全过。教训:**给 cordis 树加运行时 mutation 时,先想根载体/组节点**——entry.update 的语义对它们不是「关一个插件」而是「拆整棵树」。
- 2026-08-26 **v0.11.10 已发布**(commit c973447 + tag,Release id 377038129,exe 181MB+blockmap+latest.yml,302 校验 OK;发布脚本 .claude-ui/release-0.11.10.js 未入库):修「设置→Agent 预设整页空白 + Harness 新会话无法创建」。根因:官方 apps/cli 的 composeProfile(profile-boot.ts SHIPPED_PRESET_ROOT)会把随部署发布的 preset 根(apps/cli/config/agent-presets:standard/code/minimal/cordis)以 system 信任补进 agent-presets 行,而 harness-bridge 手工组装补丁层时漏了它——花名册只剩空用户根($DSH_HOME/.agent-presets),前端 ui-agent-preset 对空 roster 按「unavailable」渲染 null 整页隐藏,默认 preset 'standard' 解析失败导致 session.create 报 agent-preset-invalid。修复(harness-bridge drafterOverlay):{ id:'agent-presets', config:{ default:'standard', roots:[{path:HARNESS_ROOT/apps/cli/config/agent-presets, trust:'system'}] } }(patch config 整值覆盖,default 必须补回;includeUserRoot 默认 true 不受影响)。**连锁修复**:standard 挂载时 tool-web→turndown(CJS)→@mixmark-io/domino 加载失败——①vendor-deps 漏收 domino(从 node_modules/.pnpm 拷入 vendor-deps/@mixmark-io/domino,注意 vendor/.gitignore 的 lib/ 会排除,须 git add -f);②domino package.json main:'./lib' 指向目录,cjsIndex 原样收目录路径导致 require EISDIR——registerCjsPackage 增加「main 是目录则落到 index.js/cjs/mjs」。打包:apps/cli/config/** 加入 asarUnpack(cordis preset 的 skill-filesystem 需真实目录)。探针 .claude-ui/probe-agent-presets.js(纯主进程 RPC,开浏览器弹窗前先跑它):四套预设逐一 session.create 挂载通过 + agentPreset.select(minimal) 切换 OK;npm test 152/152。教训:**照搬官方功能时,对照 apps/cli 的 composeProfile/composeEntries 拼一层层核对哪些「应用侧事实」(发布根/distIndex 等)被我们的手工 boot 漏掉**。
- 2026-08-26 **v0.11.11 已发布**(commit ccc53ac + tag,Release id 377044987,exe+blockmap+latest.yml,302 校验 OK;发布脚本 .claude-ui/release-0.11.11.js 未入库):工作区选择器支持盘符切换。根因:官方 browse 目录选择器的面包屑把主目录之上的祖先全部折叠(displayCrumbs 只从「主目录」起显示),Windows 下够不到盘符根,无法切 D: 等盘。修复:①host directory-picker-browse——盘符根级(如 D:\)的 crumbs 换成探测到的盘符列表(stat 探测跳过未挂载字母),该层子目录照常列出(不返回空面板);②client DirectoryBrowser displayCrumbs——win32 下主目录之上保留盘符根 crumb(C:\ › 主目录 › …)。e2e probe-session-create.js(主目录→C:\→D:→ClaudeUI→打开→注册工作区→新会话)8 断言全过;probe-drives.js 4 断言;vendor 两包 104/104;npm test 152/152。注意:vendor lib/ 实际不入库(vendor/.gitignore 的 lib/ 规则覆盖根 .gitignore 意图),打包用工作区文件而非 git——改 vendor 源码后必须本地 tsc+tsdown 重建再 npm run dist。另:本次新会话失败的真根因已由并行会话在 v0.11.10 修掉(Agent 预设发布根缺失)。
- 2026-08-26 **v0.11.12 已发布**(commit 4d8ba73 + tag,Release id 377089615,exe+blockmap+latest.yml,302 校验 OK;发布脚本 .claude-ui/release-0.11.12.js 未入库):修「Harness 选择工作区/新会话点击无反应,静默停在 hero」。两个叠加根因:①**attachments stub 的 imageLimits 非法**(harness-bridge prepare 里 ctx.provide('attachments') 填了 {maxMessageImageBytes:0, maxMessageImages:0, maxImageBytes:0, allowedTypes:[]})——apiproxy 把 imageLimits 注册成投影,经 imageLimitsProjectionSchema(host/apiproxy/src/api/sessions.schema.ts)校验要求全正整数+mediaTypes 数组,校验失败 → session.history 返回 internal error → 前端会话 open 流程中断,一切「创建/选择」都死在这一步且界面无提示(失败只进 console.warn)。修为合法上限+mediaTypes:[](空数组=不允许图片,上传入口不展示、admission 必拒)。②**打包态 preset 挂载因子路径导入失败**:standard 预设 delegation 组有 `@deepseek-ai/dsh-tool-subagent-control/list-agents` 子路径导入,shimImport→resolveAny 的包索引只认精确包名,开发态靠 pnpm 符号链接锚点兜底能过,打包态无符号链接 → Cannot find module → session.create 报 agent-preset-invalid。修为 shimImport 对 @deepseek-ai/* 先走已注册的全局 ESM resolve hook(resolve-hook.mjs 有 exports 子路径/通配映射),失败再回退多锚点。**教训:①改宿主侧任何服务占位/stub 时,取值必须过宿主 schema——「不会用到」的字段也会被投影校验;②打包态与开发态模块解析路径不同(pnpm 符号链接),preset 挂载类功能必须在打包态(win-unpacked 直跑+CDP)验证,探针:.claude-ui/probe-packaged-wsflow.js(复制真实 harness home+隔离 userData,驱动 webview 点击并截图);③宿主侧排查利器:ipcMain.handle/ipcMain.on 包装拦截 harness:fetch/openSse 记录全量 RPC+SSE 帧(.claude-ui/probe-ws-flow-e2e.js),渲染侧包 __DSH_TRANSPORT__.fetch 无效(IpcApiClient 构造时闭包捕获 mainFetch)**。npm test 152/152。注意:composer 提示「当前模型不可用」是隔离环境无 Key,与本修复无关。
- 2026-08-31 **v0.12.0 已发布**(commit be722d49 + tag,Release id 379651069,exe 182.7MB+blockmap+latest.yml,302 校验 OK;发布脚本 .claude-ui/release-0.12.0.js 未入库):修 Harness 首条对话 `PI_AI_ERROR: Cannot find package '@anthropic-ai/sdk' imported from ...vendor-deps/@earendil-works/pi-ai/dist/api/anthropic-messages.js`，以及补包后 Kimi Coding 的 404。根因:electron-builder 打包态只可解析 vendor-deps；此前只收 pi-ai 本体却漏掉其 lazy 模型适配器在真正首条消息动态 import 的依赖闭包(@anthropic-ai/sdk@0.91.1、openai@6.26.0、http(s)-proxy-agent、json-schema-to-ts、agent-base/debug/ms、@babel/runtime、ts-algebra)。另有两类解析陷阱:①vendor/.gitignore 的 `lib/` 会排除新依赖的运行文件，必须 `git add -f`；②@babel/runtime 无根 main、仅 `./helpers/*` exports，原 ESM/CJS entry 索引不收它，子路径加载失败——resolve-hook 加 dirs 包目录索引，harness-bridge 的 CJS hook 加 cjsDirIndex，子路径以包目录为基。Kimi 根因:keys-bridge 因 kind=authToken 把 `https://api.kimi.com/coding/v1` 当 Anthropic Messages，SDK 固定追加 `/v1/messages` → `/coding/v1/v1/messages` 404；apiOf 对 Kimi Coding 特判 `openai-completions`(Kimi Coding 实为 OpenAI chat completions)。test/keys.test.js 新增路由回归，npm test **153/153**；关键实证:.claude-ui/probe-packaged-chat.js 在**打包态+隔离 userData+复制真实 Key**首次发送「回复ok两个字即可」，助手真实返回成功。注意探针要 `delete env.DSH_HOME`，开发 shell 若残留该变量会覆盖 DRAFTER_USERDATA/harness 复制目录、误测真实用户数据；启动 exe 时 `delete env.ELECTRON_RUN_AS_NODE`，赋 `undefined` 给 spawn env 在 Windows 不会清掉变量，会让 exe 当 Node 运行。

- 2026-08-31 **v0.12.0 已完成并构建(未 commit)**:画布执行内核 ComfyUI 化(通读 Comfy-Org/ComfyUI master 源码后对齐)。①画布持久化换 API 格式({id:{class_type,inputs}},与 ComfyUI 工作流互通;migrations 0.12.0 转换存量+留 .bak,真实数据验证过);②canvasGraph.js 新模块(fromDrawflow/toDrawflow 互逆/validate 循环路径/祖先签名缓存键);③canvasJobs.js 整图运行器(就绪集推进+单点失败不炸分支+增量缓存签名一致复用产物);④渲染端「▶ 运行」按钮+节点状态环(黄/绿/蓝/红/紫)+双击画布空白搜索加节点。npm test 163/163;CDP 冒烟 20/20;dist\Drafter Setup 0.12.0.exe 已构建(check-asar 全过)。坑:isLink 必须区分 [id,socket] 与 models 值数组;prompt 必需槽空槽不落 null(否则吃掉标量 prompt);resolvePromptPreview 沿 prompt 槽递归(生成节点可作上游文本源)。

- 2026-08-31 **v0.12.1 已发布**(commit 0929e4b2 + tag v0.12.1 已指到该 commit,Release id 379679107,exe 182.7MB+blockmap+latest.yml 已上传,下载 URL 302 校验 OK;发布脚本 .claude-ui/release-0.12.1.js 已入库):即上条画布 ComfyUI 化内容(API 格式+整图运行队列+增量缓存+节点状态环+双击加节点)。版本号让位:v0.12.0 tag 被并行会话的 Harness 修复(commit be722d49)占用,画布升级改占 v0.12.1(已 commit 两次:b8b57a6d 版本让位 bump、0929e4b2 发布脚本)。npm test 163/163;check-asar version=0.12.1 全过。坑:并行会话共建时,tag 可能已被推远端但指向旧 commit,发布前用 `git ls-remote --tags` 核对 tag 指向,必要时 git tag -f + push -f 让 tag 跟上含发布脚本的 HEAD。
